# Project status & partner guide — 2026-09-15, updated through 2026-09-25

This is the one document a **cold-start partner — AI or human — reads
first**. It maps everything the project has done, what is live and
verified today, how to operate and extend it, and where the deep
documentation lives. Everything here is shareable; material we keep
private (Sony-copyrighted archives, credentials) is *not* documented
here — it lives in a sibling offline folder with the owner (see
[Private material](#private-material)).

If you are an AI partner: read this file end-to-end, then the doc map
at the bottom, then the *Rules of engagement* section twice. Those
rules are hard constraints, not suggestions.

## The mission

Port a VLC-class media experience onto pre-Android, Linux-based Sony
BRAVIA TVs — right-to-repair work on owner hardware. The sets are
phenomenal panels (X-Reality PRO, active 3D) that Sony EOL'd: smart
services dead, firmware downloads pulled. The goal is an owner-run
LAN stack that gives these TVs capabilities Sony's dying cloud never
delivered, plus the longer arc of understanding the platform well
enough to run owner-chosen software on it (see
[feasibility roadmap](feasibility-roadmap.md)).

## Test hardware (both on the LAN, DHCP-pinned by MAC)

| Model | Year | Chassis | LAN IP | Role |
|---|---|---|---|---|
| KDL-46EX725 | 2011 | AZ2-F ("BATV") | 192.168.0.22 | **live test target** |
| KDL-46HX855 | 2012 | AZ3F ("ATREYU") | 192.168.0.21 | **never crash-test** — it is the workstation's HDMI monitor |
| d2server | — | Debian 12 | 192.168.0.60 | media server (Serviio 2.5, all app services, media disks) |

The EX725 is the test bed for everything. The HX855 is the better
set (12-bit, active 3D) and gets changes only after they are
owner-verified on the EX725.

**The measurement workstation behind the HDMI/KMS lanes** (2026-09-22,
recorded once so the A/B docs stop saying "the same card"): ASUS TUF
GAMING X570-PLUS/BR board, Ryzen 5 5500G (Cezanne — its Vega iGPU is the
AMD control head in every A/B) plus a GALAX GeForce RTX 3060 (GA106,
12 Gb VRAM); NVIDIA driver 615.71.09 on kernel 7.0.10+deb14.

## Where the project stands (2026-09-18)

**The media experience is DONE and owner-verified live.** Both TVs
browse the full Serviio library on d2server via an era-lean web app
and play:

- **Every audio format in the library** — mp3 native; flac/wma/m4a/
  wav/ogg through a live transcode pipe; mpc/wv (which Serviio cannot
  serve at all — no DIDL `res`) resolved by title/duration and piped
  too. Owner-verified per-format on the TV.
- **Every video format that matters** — MP4/H.264 direct (up to
  1080p High@L4.0 SBS 3D); MKV/AVI/WMV/WebM/RMVB via an on-demand
  cached faststart-MP4 transcode lane; mpg/mpeg direct. Dolby AC-3/
  E-AC3 bit-exact through the set's own decoder. Owner-verified.
- **Player UX (rewritten 2026-09-18 for the era's real limits)** — the
  player is now the set's own native transport, enlarged ~3x with
  `-o-transform:scale()` for couch distance, cover art as the poster.
  Our JS owns only what the native bar cannot do: prev/next in folder
  (wrap-around), repeat-this-track persisted in a dated cookie (the
  browser dies on every input change, so state must survive page
  reloads), and back. A `/manual` page on the TV documents the keys
  and codecs in the user's language. Design history and the measured
  platform facts behind it: `era-media-element.md`,
  `era-key-vocabulary.md`, and the public method guide
  `build-your-own-bravia-portal.md`.
- **The remote's real key model is now measured, not guessed** (see
  the key section below) — the multimedia keys do not exist as
  keydowns on this platform; the on-screen native transport is the
  only transport an app can have.

All of it runs as **systemd services on d2server** under a single
bucket target, surviving reboots, with a config file making the
install portable. That is the "VLC on a 2011 TV" milestone: reached.

Alongside it, research lanes continue: firmware analysis, the
widget/applicast lane, Presto engine CVEs, kernel survey, and the
right-to-repair sharing plan.

## The live media stack

```
   EX725 (.22) / HX855 (.21) — era Presto browser
        │  plain HTTP, arrow-key nav, CERS sendText delivery
        ▼
   d2server (192.168.0.60)
   ├── :8090  tv-mediabrowser (bravia-mediabrowser.service)
   │            ├─ UPnP SOAP browse ──> Serviio :8895
   │            ├─ /stream/  Range/206 proxy (same-origin trick)
   │            ├─ /atr/    live audio transcode pipe (ffmpeg → MP3 320k/48k)
   │            ├─ /tr/     on-demand video transcode → cached faststart MP4
   │            └─ /keys    remote keyCode probe (logs KEYPROBE lines)
   ├── :8443  rd1 HTTPS portal (bravia-rd1-probe.service, TLS1.0-era)
   ├── :8895  Serviio 2.5 media server (serviio.service, /opt/serviio)
   ├── :23423 Serviio console REST (renderer profile assignment — scriptable)
   └── :80    Apache (portal pages; era-TLS vhost for the TV homepage lane)
   media:     /mnt/arquivos2 (Música + Vídeos, CIFS-shared)
```

Key architectural facts a partner must not re-learn the hard way
(all live-proven, see `tools/serviio/tv-mediabrowser/README.md`):

- The era browser can't run SPAs (no CORS, tiny JS heap, "page too
  big to display") — everything is **server-rendered HTML** with
  small inline JS.
- The era player accepts exactly **progressive faststart MP4**; live
  TS (what stock DLNA transcode serves) is refused — that's why the
  transcode lane lives app-side, not in Serviio.
- Serviio binds `res` URLs to the browsing client's IP → the app
  **proxies media through itself** (`/stream/`), browse client ==
  fetch client.
- Serviio's own MediaBrowser is Pro-walled (errorCode 554) and
  unusable on the era browser anyway; we keep its structure and swap
  the delivery to plain UPnP ContentDirectory (port 8895, no auth).
- The era player sends `Range: bytes=0-` and expects **206** on the
  video lane.

### Files

| Path | What |
|---|---|
| `tools/serviio/tv-mediabrowser/server.py` | the whole app (one file, stdlib only) |
| `tools/serviio/tv-mediabrowser/config.ini.example` | config template — copy to `config.ini` |
| `tools/serviio/tv-mediabrowser/README.md` | app deep-doc: architecture, lanes, formats |
| `tools/serviio/tv-mediabrowser/tests/` | 4 regression batteries (see below) |
| `tools/systemd/` | the two bravia units + the bucket target + deploy README (serviio.service and apache2.service are stock distro units, deliberately not rewritten here) |
| `tools/serviio/user-profiles-3d.xml` | Serviio renderer profiles (see Serviio side) |
| `tools/bravia_ircc.py`, `tools/bravia_sei3d.py` | remote control + SEI 3D injector |

## How to operate

### Deploy layout (d2server)

App code lives at `/home/pvpgn/tv-mediabrowser/` (server.py,
config.ini, README, cache/, server.log), rd1 portal at
`/home/pvpgn/rd1-portal/`. Serviio at `/opt/serviio` (profile at
`/opt/serviio/config/user-profiles.xml`). Units are in
`/etc/systemd/system/` — `tools/systemd/README.md` is the deploy and
migration procedure.

### Service control

```bash
systemctl status bravia-stack.target        # the bucket
sudo systemctl restart bravia-mediabrowser.service
```

`bravia-stack.target` wants `bravia-mediabrowser.service`,
`bravia-rd1-probe.service`, `serviio.service`, `apache2.service` —
one handle that pulls up the whole TV lane at boot.

**Restart safety rule (hard):** before restarting anything that
serves the TVs, check nobody is streaming — the stack serves on
:8090/:8443 (app + portal), :8895 (Serviio, both TV render paths and
the app's browse), and :80/:443 (Apache):

```bash
ss -tn state established '( sport = :80 or sport = :443 or sport = :8090 or sport = :8443 or sport = :8895 )'
```

**Kill/start rule (migration-era, hard):** under systemd use
`systemctl restart <unit>` and this rule mostly doesn't apply — but
if you ever must kill a process by pattern (manual migration runs,
nohup-era processes), the kill and the start must be TWO separate
SSH calls, and the plain process pattern must not appear anywhere in
the same command line — `pkill -f "server.py 8090"` in a command
whose own text contains "server.py 8090" kills your SSH session
mid-command (this happened; exit 255). Use bracket patterns
(`pkill -f "[s]erver.py 8090"`) and even then verify the string
isn't in your own command line. Also note a pkill against a
systemd-managed process just triggers Restart=on-failure in 3 s —
restart through systemd, not the pattern kill.

### Config surface

Everything host- and install-specific is in `config.ini` next to
`server.py` (see `config.ini.example`), overridable with
`BRAVIA_CONFIG=/path`. Precedence per option: **env var > config
file > built-in default**. Sections: `[app]` bind_host/port,
`[serviio]` host/port, `[library]` roots/cache_dir, `[keys]` the
measured key table (see the key section above — the era media keys do
not exist as keydowns, so the block maps what does arrive). A CLI port
arg still wins over the file (the systemd unit passes 8090). On
startup the app prints the resolved config path, media roots and
keymap to `server.log` (the unit runs Python unbuffered so this appears
immediately).

### The remote's real key codes — measured, not guessed

The original `[keys]` defaults were Android-TV-family **guesses**. The
`/keys` probe page plus glyph/font/cookie probe pages settled it on
the EX725 (2026-09-18): **the multimedia keys produce no keydown at
all** — PLAY, PAUSE, STOP, PREV, NEXT are simply not delivered to the
page. What arrives is `13` OK, `37` left (and REW), `39` right (and
FF), `38` up, `40` down; the four colour keys belong to Opera (GREEN/
YELLOW = history back/forward, RED/BLUE = page bottom/top) and are not
bindable. The `[keys]` block in `config.ini.example` now carries the
**measured** table, and the full map — keys, glyph coverage, the
Medium-font caveat — lives in
[era-key-vocabulary.md](era-key-vocabulary.md). The on-screen native
transport is the only transport this platform can give an app; that is
why the player was rewritten around it. `/keys` stays: run it on any
*other* set before trusting these numbers there.

### Regression batteries

Four batteries cover the app's live-proven behaviors. Run from the
`tv-mediabrowser` directory:

```bash
BRAVIA_SKIP_INDEX=1 python3 tests/test-config-keys.py    # config surface + keymap + /keys
BRAVIA_SKIP_INDEX=1 python3 tests/test-player-ux.py      # prev/next/repeat player UX
BRAVIA_SKIP_INDEX=1 python3 tests/test-mpcwv-fix.py     # mpc/wv resolve + relative URLs
BRAVIA_SKIP_INDEX=1 python3 tests/test-verify-fix.py     # rmvb/webm index + mime table
```

They monkeypatch the UPnP browse so they run offline; each prints
`RESULT: ALL PASS`. Run them after any server.py change. They are
also the fastest way for a new partner to understand the app's
contract: each check names a live-proven behavior.

## Serviio side

- **Renderer profiles** (`tools/serviio/user-profiles-3d.xml`,
  deployed at `/opt/serviio/config/user-profiles.xml` — `/opt/serviio`
  is a symlink to the current install, `serviio-2.5/`):
  - `sony2011x` (extends stock `sony2011`, which extends `sony2012`):
    the daily driver for both
    TVs — raw delivery for everything the sets play natively, LPCM
    for the lossless oddities, and the **3D fix** — H.264 SEI frame
    packing insertion so MVC 3D titles play (the reason the repo
    exists; see `tools/serviio/serviio-3d-explainer.md` and the
    forum-post drafts in the same dir).
  - `tvbox-vlc` (deployed 2026-09-15, verified by a 6-agent adversarial
    workflow, 0 findings): direct-pass profile for the two Android TV
    boxes on the LAN (.24/.25) that run modified VLC — everything
    deliverable raw, Audio→lpcm only for mpc/wv/ape. Assigned to both
    boxes via the console REST API after a full Serviio restart; the
    TVs' sony2011x assignments survived untouched.
- **Console REST API** (found by reading serviio-web-console.jar):
  `GET/PUT http://127.0.0.1:23423/rest/status` with an
  `Accept: application/json` header returns/accepts the renderer
  registry — `PUT` with `renderers[].profileId` changed is exactly
  Console > Status > Devices > Save. `POST /rest/action {name,
  parameter}` for RPC (stopServer/startServer). This makes renderer
  assignment scriptable — no GUI needed. (Port 23423 is the console
  REST listener; 23424 is the API/mediabrowser listener.)
- **Force a library refresh** (after adding files):
  `POST /rest/action` with `name: forceLibraryRefresh`.

## Research lanes — state of each

| Lane | State | Docs |
|---|---|---|
| Platform identification | DONE — MIPS mipsel "ATREYU", glibc 2.7, DirectFB/Qt/Opera, Linux 2.6.35-era | `platform-map.md` |
| Live CERS/IRCC remote API | DONE, tool `tools/bravia_ircc.py` | platform-map |
| UPnP DMR playback | DONE — Serviio renders to both sets | liverecon notes |
| LAN media MVP → full app | DONE — this stack, owner-verified | tv-mediabrowser README |
| 3D lane (SEI injector) | DONE — `bravia_sei3d.py`, ffmpeg wrapper, profile fix; batch conversion runs server-side | serviio-3d-explainer |
| 3D-signalling campaign | **Pipeline end-to-end + serving side engaged.** Encode→remux→player chain (2026-09-16): HandBrake **PR #8100 merged** (x264 frame-packing SEI, closes their #5826); ffmpeg [#24530](https://code.ffmpeg.org/FFmpeg/FFmpeg/issues/24530) + [#24531](https://code.ffmpeg.org/FFmpeg/FFmpeg/issues/24531) filed, then **[PR #24628](https://code.ffmpeg.org/FFmpeg/FFmpeg/pulls/24628) OPEN 2026-09-22, review round 1 answered 2026-09-23** (jamrial caught that VVC never decoded the SEI; his decode function folded in as his own commit, our overclaim corrected in the commit message and description, FATE rerun); sibling **[PR #24643](https://code.ffmpeg.org/FFmpeg/FFmpeg/pulls/24643) OPEN 2026-09-23** fixes #24530's EEXIST abort — coded SEI wins by default, container on request; #24531's bitstream-filter insert is next, held for pacing) — h264/hevc decoders export the frame-packing SEI as stereo side data and track it mid-stream, FATE-samples verified in a trixie container, materials zip attached; scope comment-64303 records that no other open issue is solved by it (swept six term sets; only closed #24269 and unrelated #20403 surfaced); StaxRip #1873, x265 #970 posted; mpv [#18489](https://github.com/mpv-player/mpv/issues/18489) + [PR #18490](https://github.com/mpv-player/mpv/pull/18490) **in review** (**MERGED 2026-09-23 13:41 UTC by kasper93**, merge `bdefd6cb42`, #18489 closed; our commits `6c59f7d761` and `a860bc42ba` are on mpv master, and kasper93 added three follow-ups of his own on top. Before merging, kasper93 took the PR over for landing, rewriting both commits in mpv style with Daniel as author and himself as co-author, which started the CI that first-time-contributor pushes could not; every OS passes except a FreeBSD compile failure that master does not show, under investigation. Earlier the same day Daniel answered factually on #18490 and raised a Code of Conduct concern with VideoLAN; a VideoLAN contributor had cross-posted to #18490 presenting `ai-skill/SKILL.md` as the prompt behind the MRs and linking a fork of this repository; the file is a contributor guide first written after the PR and its replies, and now opens with a note saying so (`62c3496`); details in the tracker, row 5) (hostile start → 8 technical threads answered same day in the owner's own words; CI still never ran — first-time-contributor gate; follow-up context comment posted on #18489); BD3D2MK3D videohelp thread [closed out](https://forum.videohelp.com/threads/395498-BD3D2MK3D-Convert-3D-BDs-or-MKV-to-3D-SBS-TAB-or-FS-MKV-Support-thread/page21#post2803796) with r0lZ's cross-brand confirmation; two LTT audience posts (3D-theater [16936161](https://linustechtips.com/topic/1589907-i-built-a-3d-theater-in-my-basement/?do=findComment&comment=16936161), Steam Frame [16936512](https://linustechtips.com/topic/1642726-the-steam-frame-changes-everything-full-review/?do=findComment&comment=16936512)). Second wave (2026-09-18), the serving side: mkvmerge [Codeberg #6309](https://codeberg.org/mbunkus/mkvtoolnix/issues/6309) **filed as the owner** (no AI disclaimer per mbunkus doctrine), progressed to merge: **!6311 (AVC) MERGED 2026-09-21**, !6312 (HEVC) in final review; Jellyfin [PR #18060 comment](https://github.com/jellyfin/jellyfin/pull/18060#issuecomment-5726381078), UMS [#6329](https://github.com/UniversalMediaServer/UniversalMediaServer/issues/6329), Gerbera [#3937](https://github.com/gerbera/gerbera/issues/3937) (no-remux SEI injection). **All upstream threads are email-trigger watch only — never poll.** Ecosystem evidence base: `3d-signalling-ecosystem.md` | `docs/upstream/media-stack/`, `docs/3d-signalling-ecosystem.md` |
| HDMI/KMS stereo output | **Upstream outcome 2026-09-23: both nouveau series WITHDRAWN.** Lyude Paul (nouveau maintainer) declined them: another developer is implementing the same features by hand, nearly complete, including IMP, **display bandwidth allocation (without it, deep colour at higher resolutions can crash the GPU on modeset; our 225 MHz sink never exercised that)** and HDMI DSC/FRL; parts already merged. Daniel withdrew both series, offered them as a proof of concept, and offered this bench for a Tested-by on the other developer's work. The measurements below stay true of our bench; they are no longer an upstream submission. **The other developer is Mohamed Ahmed** (`mohamexiety/nouveau` on freedesktop): his unposted branch `nouveau-imp-upstr-v120` adds IMP validation, `max bpc` and HDMI deep colour on **Turing (12 bpc) and Ampere (10/12 bpc)**, so the RTX 3060 is covered, not only Blackwell; it handles the GSP GCP rewrite and frame-packed 3D. It lacks CD=5 at 10 bpc, limited-range RGB and YCbCr. **Our contribution to it:** a two-line CD=5 commit (compiled on his branch; copy `docs/upstream/mohamexiety-nouveau-gcp-cd5-10bpc-2026-09-23.patch`), waiting on freedesktop account verification [#4030](https://gitlab.freedesktop.org/freedesktop/freedesktop/-/work_items/4030) to open the MR on his branch; **Bench runs of his branch (2026-09-23, test kernel 7.3.0-rc1 booted once, automated, then removed):** run30 HX855 and run31 KDL-46EX725 over a **10 m cable** (second same-generation model; EDIDs identical in capabilities) displayed 12, 10 and 8 bpc and 3D at 12 bpc including 1080p24 frame packing at 222.75 MHz; run32 A/B at 10 bpc (frames tagged `CD-ON`/`CD-OFF`): both showed 10-bit, so **our CD=5 fix is spec compliance only on these sinks**, and the MR text will say so; his branch is RGB only, so YCbCr could not be tested. **Colour-format port (2026-09-23 evening, Daniel's go):** our withdrawn colour-format series was ported onto his branch as a local proof of concept, branch `ycbcr-poc` in `/K3D/temp/mohamexiety-nouveau` (tip `396725d9d`: the CD=5 fix plus six patches; YCbCr depth selection reuses his `nv50_hdmi_fix_bpc()` and TMDS fitting; 4:2:0 on GA102+ also passes the head's 4:2:0 state to his IMP query and is untested, since neither Sony declares 4:2:0). **run33 (EX725, 10 m cable) and run34 (HX855): all 22 steps pass on both** (RGB full/limited/automatic at 12/10/8 bits, YCbCr 4:4:4 and 4:2:2 at 12/10/8, SBS/TaB/FP 3D each switching the set by itself, 720p, 576p/480p on the BT.601 matrix, 640x480). run33's journal held 8020 per-vblank warnings in `gv100_head_state` during 4:2:2 (the readback lacked the 4:2:2 depth codes; picture unaffected), fixed in patch 4; run34 has none. Interlaced is not testable from either PC card (nouveau disables it on Volta+ since `8ba9249396be`; nvidia and amdgpu list no interlaced mode for these sets). The port is offered to Mohamed alongside the CD=5 MR, not pushed. The test kernel is installed again and kept dormant (GRUB default 7.0.10, service gated on the kernel version), at Daniel's call. **Correction on record:** the HX855 (and EX725) **has** a VCDB with QS=1; "no VCDB" in the design note and two list replies was wrong, so the range mismatch never occurred on this bench (Daniel: no list correction, the series are withdrawn). **HDR, sourced:** CTA's HDR10 Media Profile requires YCbCr 4:2:0 (plus PQ, BT.2020, 10 bits, ST 2086 metadata); the HDMI link also allows RGB; so the YCbCr formats missing from stock nouveau and from his branch are a real HDR10 prerequisite, and the gap our proof of concept covered. Stock nouveau was re-verified 3D-capable at 8 bpc for clients that opt in (runs 3-5). MR waits on freedesktop account verification (#4030). **AMD side unchanged:** our contribution there is the Tested-by on Adrian Betschart's v3 (2026-09-21), acknowledged privately by Adrian; the thread is quiet since then (no v4, no maintainer reply; rechecked on lore 2026-09-25: amd-gfx thread still 7 messages, last one Daniel's of 2026-09-21, and nothing on the series in dri-devel or anywhere else on lore); our own AMD patches stay staged, never sent. **Privacy rule adopted:** no personal email addresses in public files (Mohamed's removed from the tip commit and history; others replaced by names in the live files; history left as is to keep the commits cited in Daniel's VLC note valid) **Golden standard reached on nouveau (with our series): 12-bit full-range RGB in frame-packed 3D, 1080p24, at 222.75 MHz against the set's 225 MHz TMDS ceiling** (run 28 step `fp bpc12 fmt=rgbfull`, driver line `fmt=RGB bpc=12 limited=0 rate=222750 kHz`, owner-verified on the OSD). That is the best signal this set can take: 1080p60 frame packing would need 297 MHz even at 8-bit. Also passing in 3D at 12-bit: SBS RGB limited and YCbCr 4:4:4, TaB RGB limited, FP YCbCr 4:4:4 and 4:2:2 (runs 27/28). The proprietary NVIDIA stack misses both halves (no 3D modes, deep colour trained to 10-bit). **Driver 3D map (2026-09-23):** i915 allows HDMI stereo since 2013 (`573e74adbd13`); stock nouveau was 3D-complete (its gaps were deep colour and colour formats, now fixed); NVIDIA proprietary and amdgpu were the missing 3D pieces; RK322x and Allwinner T10 vendor kernels drive HDMI 3D (measured in sparky-armbian); mainline dw-hdmi sends the VSIF but `drm_bridge_connector` leaves `stereo_allowed` as a TODO. **SBS-half and full frame packing, picture+signal, now proven on BOTH patched amdgpu and stock nouveau.** Patched amdgpu: 22/0 → 51/29 modes and SBS-half 1080p60 auto-engages with visible depth. Run 8's 1080p24 FP attempt auto-engaged 3D but displayed black (SIGNAL PASS / IMAGE FAIL), isolating DC's un-expanded link timing; run 10 with the `CRTC_STEREO_DOUBLE` timing patch displayed the disparity picture through the AMD link: **FP picture+signal now proven on amdgpu too**. Stock nouveau on GA106 delivers the same FP buffer with visible depth. Physical topology: AMD and NVIDIA cables occupied two inputs of the same BRAVIA; both inputs were observed in 3D during the AMD window (AMD black, NVIDIA lower-frame fragment retained from its preceding FP path). This proves independent HDMI links can carry the standard signal and qualifies the future AMD→EX725 plus NVIDIA→HX855 two-TV topology. Future debugging is isolated one link at a time: AMD harness removes only `nvidia_drm`, preserves CUDA containers, blanks other AMD CRTCs, then restores the display leaf. DC still leaves `timing_3d_format=NONE` despite containing FP timing/view/OPP machinery. Dual-surface composition is independently proven in wiz3D. **Upstream strategy pivoted 2026-09-20**: the "issues our patches solve" sweep found zero drm/amd GitLab issues but a live independent series — Adrian Betschart's `[PATCH v3 0/3] drm/amd/display: HDMI 1.4 3D output ...` (amd-gfx, 2026-09-07, DCN 3.2.1 + JVC projector, no maintainer reply yet) covering the same two deltas. Rather than fire a competing series, Daniel approved a Tested-by/evidence reply on that thread carrying our complementary record (DCN 2.1 + consumer Sony TV: run-8 signal-pass/image-fail boundary, run-10 picture pass, nouveau cross-vendor control). Reply raw material: `docs/upstream/reply-draft-betschart-thread.md`; our two forward-verified mboxes (`docs/upstream/0001/0002-*.patch`) stay staged as DCN 2.1 insurance. Terminology for posting: amd-gfx is the list, amdgpu the one modern driver (GCN 1.0→RDNA), radeon the pre-GCN one. **NVIDIA lane, measured success 2026-09-22** — the open-gpu-kernel-modules #1382 exchange, measured end to end: the six-line `stereo_allowed` patch alone is necessary-but-not-sufficient (run18, `NvKmsKapiDisplayMode` carries no stereo field, so the MODE_NO_STEREO gate has nothing to admit); v2 parses the sink EDID's HDMI VSDB 3D fields inside `nv_drm_connector_get_modes()` and synthesizes 3D-flagged variants by cloning only base timings NVKMS itself listed (a byte-verified mirror of `drm_edid.c`'s `do_hdmi_vsdb_modes()` + the CTA mandatory stereo table). **run19: stock 17 modes/0 stereo in every client-cap combination; v2 39 total / 22 stereo with STEREO_3D, 17/0 without** — the opt-in contract holds exactly and no synthesized mode was rejected at validation. Roster: 7 frame packing, 8 top-and-bottom, 7 side-by-side half. The entire remaining gap to nouveau (51/29, same EDID) is the 1080i family: the sink declares frame packing on VIC 20/5 (1080i 50/60 — broadcast 3D's formats) but NVKMS lists no interlaced base timing, so those modes are unreachable from the open glue by construction — the second NVKMS-list gap, sibling to the measured 10-vs-12-bit deep-color narrowing on the same sink (Windows on the same card and set trains 12: driver-side policy). Design + byte-level parse + measured table: `docs/upstream/nvidia-615.71.09-hdmi-3d-vsdb-synthesis-design-2026-09-22.md`; patch: `nvidia-615.71.09-hdmi-3d-community-v2.patch`; log: `tools/stereo-modeset/run19-*.log`. **Owner's framing, recorded because the evidence supports it (his words):** this campaign keeps surfacing solutions that went uncovered for years — the `stereo_allowed`→NVKMS chain has no public record anywhere and NVIDIA's own maintainer had to ask us for a test case; meanwhile a 10-vs-12-bit color narrowing sits unpatched in the current production branch of a vendor with three decades of 3D Vision history — 'otherwise, why the color bug?' **Unified filing 2026-09-22: [#1384](https://github.com/NVIDIA/open-gpu-kernel-modules/issues/1384)** folds the three measured symptoms into one issue (mode prune, 3D absent, 10-vs-12 deep colour), argues the HDR connection from the normative chain itself (BT.2100-3; HDMI 2.0a referencing CEA-861.3; CTA-861.3-A metadata blocks; 10-bit PQ in a 12-bit YCbCr 4:2:2 wire container — honest scope note kept: no HDR display on the bench, argued not measured), and cites the independent sibling map — #1348 (same 615.71.09, HF-VSDB DSC_MaxSlices misparse forcing YCbCr 4:2:2-limited, filed by a stranger to this project ten days before our PS ever landed, still unanswered), #1369, #1184, the HDR trio #779/#933/#1285, and the #1101 fixed-in-610.43 glue precedent. Cross-linked from #1382 ([comment 5771657559](https://github.com/NVIDIA/open-gpu-kernel-modules/issues/1382#issuecomment-5771657559)) so that thread stays scoped to 3D. Asks: NVKMS-vs-glue ownership and the real EDID capability model. Whose move: NVIDIA, email trigger only | `tools/stereo-modeset/`, `docs/dual-surface-hdmi-3d.md` |
| NVIDIA/nouveau HDR lane | **2026-09-23: two of the three HDR prerequisites are fixed; the third is open.** Deep colour and YCbCr output were missing in stock nouveau and were covered by our two list series, **withdrawn 2026-09-23** in favour of another developer's hand-written implementation (see the HDMI/KMS row and `docs/upstream/issue-tracker.md` row 13); on our bench they remain the measured proof that the transport works (10/12-bit, YCbCr 4:2:2 and 4:2:0, the base HDR uses). HDR signalling itself (the `HDR_OUTPUT_METADATA`/Colorspace properties and the CTA-861.3 DRM InfoFrame) is still absent, blocked by the 17-byte NVIF packet limit described below; the HX855 is not an HDR set, so that part can be built from the specifications but not measured here. History of the lane follows. The proprietary stack's open glue already exposes `HDR_OUTPUT_METADATA`/Colorspace and hands metadata to closed NVKMS, so #1384 correctly leaves its link-depth/output-policy repair with NVIDIA. In nouveau 7.3-rc4, the corresponding KMS properties and CTA-861.3 DRM InfoFrame path are absent. The audit found the exact HDR boundary: NVIF/nvkm carry only 17-byte AVI/VSI packets, while HDR needs 30 bytes. It also found two credible open routes—Valve's new GB20x 36-byte generic InfoFrame writer and r535's 36-byte `SET_OD_PACKET` GSP command—but neither proves the GA106 slot contract yet. **Deep-colour prerequisite now mapped separately:** nouveau ignores HDMI VSDB `DC_30/DC_36` for link selection, exposes no `max bpc`, and leaves TMDS SOR depth at default even though its display classes define 36-bpp RGB. The owned HX855 makes 12-bpc SDR directly testable at 1080p60: 148.5 × 1.5 = 222.75 MHz against the EDID's tight 225 MHz ceiling. A reversible module-swap/OSD experiment is designed; this would prove transport, not HDR. **Run 2 measured 2026-09-22:** the experimental patch exposed `max bpc` (8–12) on HDMI-A-2, accepted `max bpc=12`, and committed the 1080p60 36-bpp modeset with zero driver-side errors; the sink refused the wire with its "incompatible signal" OSD — engagement end to end is proven working, and the measured missing half is the General Control Packet deep-colour indication that the plan doc flagged as GA106's open question. Owner aborted the hold deliberately (~50 s in) and rebooted; kernel/desktop restore clean, docker restore skipped because a SIGTERM-killed `sh` never runs its EXIT trap — gap recorded as a harness defect, containers restarted manually, harness hardened (TERM→exit trap + self-consuming boot restore unit). **GCP route located end to end (2026-09-22, source-only):** NVIDIA's open tree carries the exact recipe — NVKMS `SendHdmiGcp()` byte construction (SB0=CLR_AVMUTE, SB1=CD 0x6 | PP<<4, SB2=0 → `0x00002610` for 1080p60 12-bpc RGB), the GA102-class C671→9171 slot writer, and cl9171.h register truth matching nouveau's working AVI/VSI SF-aperture offsets bit for bit; nouveau's GSP path never writes the GCP slot at all, so run 2's "incompatible signal" is precisely a deep-colour stream with zero GCP declaration. **Run 3 measured 2026-09-22:** v2 GCP module drove the full path clean (EDID gate, `max bpc=12`, modeset committed, zero kernel errors; hardened harness restored docker by itself) yet the sink answered "incompatible signal" again. Post-run source cross-check in `nvkms-evo3.c` corrected the depth-carrier model: C37D/C57D `SOR_SET_CONTROL` has no PIXEL_DEPTH field at all and nouveau's sorc37d never merges the NV837D depth bits — depth reaches the hardware via the head OUTPUT_RESOURCE field, which v1 already programmed NVIDIA-conformantly; NVIDIA's `SET_HDMI_SINK_CAPS` carries no deep-colour bits and hdmipkt's GCP control write is the same bit-0 enable nouveau used. So runs 2/3 matched NVIDIA's visible recipe everywhere, and the refusal splits two ways [inferred]: (a) the CPU-side GCP MMIO write never stuck under GSP ownership of the SF aperture, or (b) GCP landed but the TMDS character rate stayed 148.5 MHz because closed RM/GSP declines to derive 222.75 from nouveau's state. **Patch v3 (instrumentation only) built:** readback printks of GCP subpack/control plus sibling AVI slot in `gv100_sor_hdmi_gcp()`, and a TMDS depth/clock line in `nv50_sor_atomic_enable()` — run 4 splits (a) vs (b). `docs/upstream/nouveau-hdmi-deep-colour-experimental-v3.patch` (cumulative); module `nouveau-hdmi-deep-colour-gcp-dbg-experimental.ko` sha `68768c48…08ded4`, vermagic-matched; subsequently loaded in run 4. **Run 4 measured 2026-09-22 (09:27:59–09:29:55 UTC-3):** owner ran the v3 module and the sink refused identically. **Run-4 audit correction:** the `0x00002610` readback occurred immediately before core commit, not after the whole transaction. Post-commit `r535_sor_hdmi_audio()` then sends a zero-depth `SET_OD_PACKET` GCP and overwrites the subpack with `0x00000010`, so the run did not prove final GCP state, wire emission, or TMDS character rate; `clock=148500` was the mode pixel clock. NVIDIA's NVKMS performs the same legacy audio operation but follows it with `SendHdmiGcp()` rebuilt from committed head depth, and that packet path also calls `CTRL_HDMI` indirectly through C671→9171. Cumulative v4 preserves CD/PP across audio and logs the final post-audio state; it clean-builds and pristine-base dry-runs, module sha `2cfde710…04c0e`, **never loaded**. Remaining levers (proprietary-oracle register diff via the `hdmi_deepcolor:bool` module param, GSP-commit-path audit, `SET_OD_PACKET` injection route, sink service-menu rate readout) collected with file:line citations in the partner handoff `docs/research/nouveau-deep-colour-handoff-to-codex-2026-09-22.md`. Native HDR still requires an HDR sink or analyzer. The cross-vendor high-precision SDR and native-HDR map now lives in [`awesome-linux-hdr`](https://github.com/danielcamposramos/awesome-linux-hdr). | `docs/research/nouveau-hdr-gap-2026-09-22.md`, `docs/research/nouveau-hdmi-deep-colour-plan-2026-09-22.md`, `docs/upstream/issue-tracker.md` |
| Deep Color run-5 closure (supersedes the final state of the row above) | **NOUVEAU 12-BPC SDR TRANSPORT OWNER-VERIFIED; HDR STILL NOT CLAIMED.** Cumulative v4 rebuilt GCP CD/PP after the late GSP HDMI-audio write. Final readback retained the required low-24-bit payload (`0x01002610` including its preserved generation bit); the HX855 OSD reported **12-bit**, and the green-to-purple gradient over black/white squares was stable—**passed with full colors (pun intended)**. This proves the post-commit audio clobber was causal. Sixteen-bpc (`DC_48`) follows the same HDMI/GCP mechanism in the clean upstream series but is untested; HDR is likewise unclaimed without an HDR sink. **Same evening, the proprietary lane closed too:** run 24 reloaded stock 615.71.09 with `max_output_color_depth=12 hdmi_deepcolor=1` (its only change) and the same sink reported **12-bit** on the NVIDIA link in clone mode—proving the default cap is the sole gate there, and measuring-sufficient the staged one-line companion patch (bbfc670)—now filed upstream as [NVIDIA/open-gpu-kernel-modules PR #1386](https://github.com/NVIDIA/open-gpu-kernel-modules/pull/1386). **And the nouveau side shipped:** the three-patch HDMI Deep Color series went to dri-devel + nouveau (cc Lyude Paul, Danilo Krummrich) as cover `<20260922192132.114546-1-Capitain_Jack@yahoo.com>` on the owner's standing pass-conditional go—checkpatch 0/0, built clean on its drm-misc-next base, hardware-verified. **Same night, v2 closed the automated review's three findings and passed the bench:** patch 2 gained the 30-bpp GCP arm (CD=5, `PP=pixels&3` of the four-pixel packing group) and the SCDC gate now thresholds the deep-color TMDS character rate; the running-kernel port (module sha `89ced700…b79`) passed run 25 (deep12 regression: 36-bpp link, renders) and run 26 (deep10 clamp: sink links 30 bpp with CD=5, renders — v1 would have declared the 24-bpp default on that wire), frames carry a big yellow run label so pictures can't be mixed up; owner-verbatim: "Perfect run partner, Both tests rung, A then B, both perfectly displayed with proper text". Send of v2 and the ack stay gated on the owner's word; the chroma/YCbCr next series is handed to the Opus lane via `docs/upstream/deep-color-opus-handoff-2026-09-22.md`. Paired nouveau/NVIDIA publication state and exact limits: `docs/research/deep-color-dual-upstream-handoff-2026-09-22.md`. | `tools/stereo-modeset/run23-nouveau-deep12-gcp-audio-preserve-pass-2026-09-22.log`, `tools/stereo-modeset/run24-nvidia-deepcolor-param12-pass-2026-09-22.log`, `tools/stereo-modeset/run25-nouveau-v2-deep12-regression-pass-2026-09-22.log`, `tools/stereo-modeset/run26-nouveau-v2-deep10-clamp-pass-2026-09-22.log` |
| rd1 portal / homepage lane | DONE — Unbound DNS override on the LAN points `rd1.sony.net` at d2server; era-TLS vhost serves the TV's homepage with a media link | liverecon notes |
| BIV / Bravia video lanes | DEAD (server-side) — registration unlocks nothing; documented so nobody retries | liverecon/biv-video-lane.md |
| Applicast widget lane | ACTIVE — **Sony's CDN is still live (2026-09-17)**: preservation sweep recovered 5 never-archived bundles + a second namespace (`/WsIndexes`, `/WsCatalogs`, `/WidgetCatalogs` incl. AZ1) across 3 hosts, 137/168 URLs answering. Key finding in `withheld-by-catalog.md`; programming model documented publicly. **Fronts archived (2026-09-17):** the load-bearing Sony fronts (AZ3 EU/BR catalogs, AZ1 catalog, bundle manifests, OSS service) now have Internet Archive snapshots beside the live URLs — table in the repo README; archive links must use `https://` (the `http://` form returns 503) | `research/appliwidget-programming.md`, `withheld-by-catalog.md`, README "Live fronts and archive snapshots" |
| Firmware decrypt | STALLED — whole-file AES, no public decryptor; 6-image corpus offline for differential work | platform-map, roadmap |
| Presto engine CVEs | NOTED — 2011-2628 and era-adjacent, no exploit built; not the current focus | `research/presto-cve-2011-2628.md` |
| Kernel survey | DONE — 2.6.35 era privesc surface mapped | `research/kernel2635-survey.md` |
| Senior-partner reviews | DONE — 3 external reviews (Opus/DeepSeek/Kimi) folded into the roadmap | `partner-review.md` |
| Right-to-repair sharing | **Wiki update POSTED 2026-09-18** — consumerrights.wiki (FULU) product-line page now carries the five dated Sony end-of-service notices (each naming both models, archived), the regional-documentation asymmetry, the browser-3D incident, the corrected firmware-removal claim (an overclaim of ours, narrowed to exactly what Sony's page supports), and a Brazil CDC context section flagged not-legal-advice. Noticeboard follow-up post drafted paste-ready alongside. Rossmann/repair.wiki outreach drafts queued, grounded in his own live Sony campaign (quotes pinned to source) | `right-to-repair.md`, `rossmann-outreach.md`, `wiki/`, `sony-end-of-service-statements.md` |
| Era browser & media element | DONE 2026-09-18 — the platform's input/render/storage model measured end to end: no media-key keydowns, colour keys are Opera's, glyph coverage (Arrows block = tofu), the `<video>` element's context rules, native controls + transform scaling, dated cookies, `timeupdate` only. The probe pages that produced it ship in the app (`/probe`, `/keys`) | `era-key-vocabulary.md`, `era-media-element.md`, `build-your-own-bravia-portal.md` |
| Browser 3D blocked | DOCUMENTED, one open lead — the panel detects the 3D signal in browser video but cannot switch (auto or manual) although the same panel switches from other inputs; i-Manual documents the feature with no input restriction. Filed as a wiki incident. Open test: does the TV's *native DLNA* player switch 3D for the same file? (that lane is not browser-gated) | `3d-blocked-in-browser.md` |
| TrackID / BGMSearch | DOCUMENTED, NOT REVIVABLE without a spare set — the buttons emit zero packets (firmware-local tombstone); the Gracenote path (SMRP → playstation.net) and the client bundle are mapped from Wayback. Deferred, not dead: an older firmware might carry the code | `trackid-bgmsearch-recovered.md` |
| Judge-by-the-cover record | DONE — the AI-policy landscape survey across the video-tooling industry (mpv, mkvtoolnix, HandBrake, Codeberg): everyone gates on owned/understood/reviewed, no one on "was AI used"; our 42-line standard-based patch is the case study. **2026-09-24, a second case:** the Half-Life 2 stereo work (stereo gaming row) was built in the open by Daniel and AI partners, provenance published, every public claim checked against a log, a frame dump or the engine source before posting; Valve's triager labelled the main report on its substance six minutes after it was filed. Daniel's framing: "we acted instead of waiting, and we shared instead of gating", the human and AI partnership as white hackers answering the "AI slop" labelling campaign with checkable work. **2026-09-25:** new section "Humans with AI": Karim Lakhani's line (HBR, 2023) that humans with AI will replace humans without AI, the week's count as evidence (21-24 September: 346 commits in 13 repositories, about 200,000 words of documentation and 6,000 lines of code, rewrites included), and Daniel's own phrase for the partnership, "We're identical in our differences (Human and AI)" | `judging-by-the-cover.md` |
| Stereo gaming: VR Stereo Spectator (Half-Life 2) | **PLAYABLE IN 3D 2026-09-24.** Daniel's first real playthrough on the HX855 (Vulkan, side by side): an old save, the canals and airboat chase, two save loads and two level transitions in one launch, no hang, no crash, shadows right ("what a blast"). Target: NVIDIA and Linux-native first (AMD here is only the iGPU, and others cover it), wiz3D (iZ3D's successor) as the reference, Proton after. `tools/vr-stereo-spectator/sourcevr/` is a replacement `sourcevr.so` implementing Valve's `SourceVirtualReality001` interface for a 3D television, public under the Source 1 SDK License with a provenance file. Daniel, with glasses on the EX725, same day: real depth, real colours, no ghosting, default calibration right (separation 2.5 units, about 64 mm; screen plane 120 units); the view follows the game; the HUD is a transparent 2D layer on the screen plane, health and ammo moved to the top; HL2's classic crosshair drawn by the module, identical in both eyes (or, with 640x480 eyes, the client's own crosshair centred); the muzzle sprite on the muzzle with `viewmodel_fov 90`. Vulkan 3D side by side: 380 fps with the view following the demo, against 585 fps Vulkan 2D (RTX 3060, 1080p). What it took: the engine never calls `CreateRenderTargets`, so the module makes its targets through the `Connect()` factory; a material first used without a main-thread precache draws as Source's purple-black error (the HUD's missing piece); the module issues the TV settings itself through the engine client interface. Mouse confined to the UI sheet through the game's SDL2 (Daniel: "perfect"). The formula for any VR engine on a 3D display: `tools/vr-stereo-spectator/FORMULA.md`; **Upstream, 2026-09-24 (Daniel approved each, posted from his account):** [Source-1-Games#8297](https://github.com/ValveSoftware/Source-1-Games/issues/8297) (the six VR-mode behaviours on a 3D display plus the OpenGL crash; pings kisak-valve, cc misyltoad), comments answering [#3782](https://github.com/ValveSoftware/Source-1-Games/issues/3782#issuecomment-5822863780) (sourcevr on Linux), [#1013](https://github.com/ValveSoftware/Source-1-Games/issues/1013#issuecomment-5822869918) (the 2013 side-by-side request) and [source-sdk-2013#268](https://github.com/ValveSoftware/source-sdk-2013/issues/268#issuecomment-5822876523) (the 2014 HUD checkerboard: no precache), and [SteamVR-for-Linux#961](https://github.com/ValveSoftware/SteamVR-for-Linux/issues/961) (stereo spectator: a side-by-side VR View mirror, and the compositor path for an OpenVR driver on a 3D display; cites Valve's "on our list" to Road to VR and LTT, with Daniel's forum answers); texts in `docs/upstream/valve/`, tracker row 17. **Valve triage, same evening:** kisak-valve labelled #8297 "Feature Request" and "Half-Life 2" six minutes after posting; misyltoad subscribed; on #961 kisak-valve, charleslvalve and aaronleiby subscribed, no label yet; #1013 is still assigned to Joe Ludwig (Valve), who took the stereo request in 2013. No replies yet. awesome-stereoscopy gained the VR Stereo Spectator entry (with DXVK) and a gamescope link in its headset-to-3DTV note; Daniel invited misyltoad (gamescope, DXVK) to the list. **Anaglyph for any colour screen (2026-09-24):** `tools/vr-stereo-spectator/anaglyph/svrtv-anaglyph.fx`, a gamescope effect (64-bit, outside the game) turning side by side into red/cyan with a "CRT" and a "modern screens" matrix, mixed in linear light; Daniel: "both perfect results". What gamescope 3.16 needed: one uniform (an effect without uniforms never runs: zero-size allocation), `--prefer-vk-device` on the game's GPU, `--backend sdl` with SDL on X11, `-g` for the keyboard. **gamescope frame race FIXED 2026-09-25** (first seen as a depth swim in anaglyph, but never an anaglyph fault): side by side through gamescope on the 3D TV swam too (p08, p09); a per-frame view-angle log (`SVRTV_ANGLELOG=1`) measured frame times of 4.0 / 33.7 ms (p5 / p95) through gamescope against 15.6 / 17.7 ms native. Two queues in a row: the game's vsync, then gamescope's nested output, hard-coded to FIFO. Cure: the game free-runs inside gamescope (DXVK `d3d9.presentInterval = 0`), and `gamescope-3dtv`, gamescope 3.16.24 built 64-bit from Debian's source with a one-line patch (`GAMESCOPE_NESTED_PRESENT_MODE`, output on IMMEDIATE; NVIDIA offers no MAILBOX for that window), in `tools/vr-stereo-spectator/gamescope/`. p16c: p95 5.7 ms, Daniel: "just like the original sbs", no tearing; p18 anaglyph modern screens p95 4.5 ms, "PERFECTION!"; p19 CRT, "also a perfect run". The module's pointer fence pinned the view inside gamescope (down-right), so it now stays off when `GAMESCOPE_WAYLAND_DISPLAY` is set (build `18d39bed`). Anisotropic 16x from the launch line (`+mat_forceaniso 16`) costs nothing measurable. Three modes from one module: native side by side, side by side through gamescope, anaglyph through gamescope (CRT and modern screens); details and table in `anaglyph/README.md`. Motion blur off while 3D is on (p20: "blur wrecked the mouse movement"; module `fcdb6730` restores the player's value when 3D stops). **The in-game 3D offer to Valve (2026-09-26, in progress):** the contribution goes to Valve's own repository as three pull requests (native side by side, side by side through gamescope, anaglyph through gamescope), each with spectator and play; fork `danielcamposramos/source-sdk-2013`, branch `stereo3d-sbs-native` (PR 1). The module now lives in Valve's tree as `src/sourcevr_display/` and builds in Valve's full SDK build (VPC, sniper image, 64-bit) and as 32-bit for today's HL2 (same source, `tier1` compiled in, HL2's own `libtier0`/`libvstdlib`, `NO_MALLOC_OVERRIDE`). New: the `-stereo3d` launch option (Steam's "3D SBS Vulkan" entry, an ask to Valve with Daniel's warning text), the saved switch `vr_display_3d` (turns 3D on and off while the game runs; while 3D is on it reads 1 whatever turned it on, and a launch-option session puts it back to 0), an additive detection interface (`SourceVirtualRealityDisplay001` through `QueryInterface`; Valve's interface version unchanged), the muzzle-sprite fix gated on it (`c_baseviewmodel.cpp`, `viewmodel_fov` stays a cheat), anisotropic 16x restored on exit like blur and crosshair. Triple buffering measured, nothing to set: DXVK's gameplay swapchain has 3 images natively (FIFO) and through gamescope; HL2's Linux engine has no `mat_triplebuffered`. The "3D display" entry sits in GamepadUI's Video options (data, `gamepadui/options.res`, tested from `hl2/custom/svrtv-3d-menu`); HL2's classic desktop Options dialog is closed GameUI code, an ask to Valve. GamepadUI in 3D: it lays out in real pixels and ignores the client's 640x480 VR UI size, so the module sizes the UI to the window when GamepadUI is on (`-gamepadui` or a Steam Deck) and does it inside the client's `Activate()` (at its `GetRenderTarget` call; a one-frame gap left the Options panel with a 640x480 layout and full-size text). Runs q01-q07 on the HX855: launch option, switch from 2D, switch off/on (fixed: 3D stays on until the client has deactivated), GamepadUI whole in 3D; Daniel: "perfect". TF2 and Source SDK Base 2013 MP (the 64-bit bed for the client-side fixes) await installation. **Gifted upstream 2026-09-26:** [gamescope#2438](https://github.com/ValveSoftware/gamescope/pull/2438) (`GAMESCOPE_NESTED_PRESENT_MODE`, built on master; cites misyltoad) and [the #8297 comment](https://github.com/ValveSoftware/Source-1-Games/issues/8297#issuecomment-5842650764) (discovery and solution). Suite: `GAMESCOPE_BIN`, `PLAY_PRESENT_INTERVAL`, `HL2BENCH_ARGS`, gamescope log per step. **Lists, same day:** awesome-vr gained the section "VR on 3D displays: the stereo spectator" (credited to Daniel) and current headsets (Quest 3S, Galaxy XR, PICO Space Pro, vivo Vision); awesome-stereoscopy gained "Proven on a VR engine, 2026", the glasses-free light-field TVs at CES 2026, current 3D monitors and projectors, and LTT's passive dual-projector build as a parts list. Open: `viewmodel_fov` is cheat-protected (for Valve: allow it for this view); OpenGL in VR mode crashes in the engine's render thread (use `-vulkan`); HL2 RTX needs its own demo. Bench: `tools/hl2-bench/` (sessions 0 and A run; B and C pending). | `tools/vr-stereo-spectator/`, `tools/hl2-bench/README.md` |
| Legacy 3D formats (act three) | **CHARTERED 2026-09-18** — enable all 3D content ever made: row-interleaved/column/checkerboard are standardized `frame_packing_arrangement_type` values (0/1/2), so conversion to SBS+SEI is standard-speaking-to-standard (`stereo3d` deinterleave → repack → SEI; transcode lane, pixels change — honest scope vs acts 1–2); anaglyph is colorimetry (CRT-tuned content on WLED panels → per-channel filter chain, plays as-is + glasses). Proper path per owner: convert (front B). First corpus: the 3D-photography community (**phereo**) material (11 JPS + SBS + anaglyph set, **private test use, community's copyright — never republished**; the owner contributes two Optimus 3D exemplars + the Gadmei T883-3D tablet and archive, and the subject depth). Also documents the YouTube 3D player withdrawal — the pattern at platform scale, silently. Results on our stack first; upstream after, same doctrine **2026-09-24: type 5 converts, interlaced 3D output not chased.** The page had said the SBS conversion cannot touch type 5 (temporal); that confused display with content and is corrected: field-sequential SD material converts with stock ffmpeg (`separatefields,stereo3d=al:sbs2l` + x264 `frame-packing=3`; `al` = left eye in the temporally first field, `ar` otherwise), at half the lines per eye and 25/30 stereo pairs a second, verified frame by frame on synthetic 576i TFF and 480i BFF clips (0 failures, SEI read back as SBS; `tools/fieldseq-3d/verify-fieldseq-to-sbs.sh`), not yet on a real disc. An interlaced 3D output path is not pursued, for five reasons recorded in the doc: no HDMI-era 3D device is interlaced-only (the 1.4 mandatory list binds sinks to progressive frame packing too); sources are not required to send interlaced and NVKMS, nouveau (Volta+, `8ba9249396be`) and amdgpu DC all refuse it; the content converts; interlace ends at 1080 (21 of the 154 CTA-861 formats in DRM's tables, none 4K/8K; Rec. 2020 progressive only); the interlaced-only 3D devices are analog CRT field-sequential kits. Measured on the bench: neither PC card lists an interlaced mode for these sets. Same explainer added to awesome-stereoscopy ("Displays decide the format": interlaced 3D, and saving field-sequential). **Real disc, same day (KDL-46EX725): PASS.** Daniel recovered field-sequential titles from Berezin's list; *Creeps* (XviD 720x480, no stored field order) was checked field-sequential first (combing ratio 3.24; field-to-field shift growing with depth, +5 to +27 px; still objects comb), eye order from parallax (top field = left), and 30 s clips in TaB (`stereo3d=al:abl`, SEI type 4) and SBS (type 3) served by Serviio **both auto-engaged 3D**, depth correct (Daniel: "the man look clearly in front, and the girl on the backplane"), TaB a little better. So these sets engage TaB from the SEI over DLNA too, and **TaB is the default packing for field-sequential material**. Test clips in `Vídeos/0 3D/Teste field-sequential/` on d2server; the other titles are still downloading and each gets the same field check before any full conversion. **2026-09-25, where the stereo uploads are:** awesome-stereoscopy gained [Stereo video on YouTube](https://github.com/danielcamposramos/awesome-stereoscopy/blob/main/stereo-video-on-youtube.md): how to watch them today, Daniel's search trick (`yt3d` plus a second term such as SBS or anaglyph; "3D" alone also pulls in computer animation), five curated viewer playlists (three more deliberately not linked because they mix in copies of studio films), and Joe Penna's 2010-2011 3D run. **Same day, asked YouTube:** Daniel posted to YouTube's Creator Liaison on X ([post](https://x.com/Daniel_Ramos_BR/status/2103357450372157932)), asking Culture & Trends whether MysteryGuitarMan was the first major creator to publish in 3D on YouTube ("3D Test" 30 Dec 2010, "3D Maestro" 1 Feb 2011, both yt3d-tagged). Framed as a question their data can answer, not a claim: we found no major creator before him, but cannot see everything from 2009-2010. Follow-up the same day ([post](https://x.com/Daniel_Ramos_BR/status/2103359043171717176)) to @YouTubeInsider (YouTube's news account, linked from its Culture & Trends page; the team's *Like & Describe* podcast has no X account and has been quiet since September 2024), @MatPatGT (the podcast's host) and Joe Penna himself (@MysteryGuitarM), quoting the first post: "I already asked @YouTubeLiaison, but would love to hear it from you too." Tagging Joe was Daniel's decision. Whose move: YouTube, MatPat, Joe. | `legacy-3d-formats.md` |
| 3D-photo gallery lane | **CHARTERED 2026-09-18, reframed same day** — the 3D photo player these sets never got (Sony's stock slideshow was 2D-only on a 3D panel). **Measured: phereo is failing** (`api.phereo.com` 504 at 60 s, twice; static assets 200 in seconds) while **[stereopix.net](https://stereopix.net/) answered 200 in 2.5 s** — so the lane is built source-agnostic, local library first (it cannot 504), remote adapters second, nothing depending on phereo being alive. Third-party reports (DPReview, photo-3d groups.io) describe phereo as long down/abandoned — cited as reports, our 504 is the measurement. Consequence: the corpus is **preservation material**, not just test material (provenance rule tightens, never loosens). Four JackDesBwa repos read: PhereoRoll3D (the working API map), PhotoRoll3D (multi-source is goal not code — one commit, no adapters; it reads as *his own* preservation response to phereo dying), StereoWebViewer + threejs-StereoscopicEffects (**both need WebGL — the era browser has none, so not one of the four can render on the set**; that closes the era-Opera question and is exactly why we render server-side). His `interleaved` output is marked "Not tested on actual device yet" since 2018 — **we own four such devices**: the contribution offer in the draft. Inherited open test **answered 2026-09-18 on both sets**: the native DLNA photo path opens the 3D menu but offers only 2D→3D, no side-by-side for stills; MPO bytes renamed `.jpg` also stay flat; the remaining door is a real `.MPO` on USB (staged). **ANSWERED 2026-09-19: MPO works from USB on both sets** — auto-engages 3D once the file is spec-correct (both views typed Disparity `0x020002`; our writer had typed view 0 Baseline Primary, which is why every earlier MPO stayed flat). **Network stays flat** (HX855 measured; EX725 was off, inferred from its identical declaration): correct MPO served byte-exact as `image/jpeg` → 2D. Both sets' GetProtocolInfo declare `image/jpeg` JPEG_LRG/MED/SM only, no MPO profile; Serviio does not index `.mpo` at all. **Software gate, proven by probe 2026-09-19** — `tools/mpo_dlna_probe.py` served the same MPO bytes four ways: `image/jpeg`+`.jpg` (control) and `image/jpeg`+`.mpo` are **listed and flat**; `image/mpo` with and without `DLNA.ORG_PN=MPO_LRG` are **not listed at all**. So the set filters its browser against its own GetProtocolInfo (profile, not capability), and the URL extension is not the dispatcher. **No DLNA-server change can fix these sets** (UMS has indexed `.mpo` since 2008, Gerbera indexes it via libmagic; both announce it as plain JPEG). Same board, same file, 3D from USB, hidden or flat over the network: a firmware target — see `3d-photos-on-bravia.md` | `legacy-3d-formats.md`, `3d-photos-on-bravia.md` |
| 3D catalog + the anaglyph inverse | **BUILT 2026-09-18** — two tools, both measured. `tools/bravia_3dcatalog.py`: detection lifted out of `ffmpeg-3d-wrapper.sh` (which already types every live transcode, then throws the answer away) and written to a JSON index with evidence + confidence + owner override per row; **first run 140 files → 122 indexed (81 anaglyph, 30 sbs, 11 jps)**. `tools/bravia_anaglyph.py`: the inverse the owner asked for, for material preserved only as anaglyph — red carries left, green/blue carry right, each eye rebuilt **only** from its own channels; **disparity recovered exactly on 11/11 corpus JPS pairs**, left luma 23–37 dB. Two traps hit and measured rather than shipped: a pointwise operator **cannot** create disparity (first attempt: perfect colour, **0 px** stereo), and naive channel-borrowing makes the two eyes **byte-identical** (1 channel + 2 channels = 3). So `--mode mono` is default and robust on any red/cyan source; `--mode color` is disparity-warped with iterative red removal, beats mono on good matches, loses on repetitive texture, off by default. Found anaglyphs are often not Dubois at all, which mono survives and colour does not. **End file**: `video` subcommand converts anaglyph movies to SBS + frame-packing SEI (verified present). Also emits a pure-ffmpeg filtergraph so the app can convert on the fly and `server.py` stays stdlib-only. Campaign loop closed: ffmpeg writes the SEI but **not** the Matroska StereoMode — our own tool hit the exact gap, and prints the `mkvmerge --stereo-mode 0:1` fix-up that [Codeberg #6309](https://codeberg.org/mbunkus/mkvtoolnix/issues/6309) asks to be automatic. **Measured blocker for the native-photo test: Serviio indexes `.mp3 .jpg .flac .wma .mkv .avi .mp4 .m4a .flv .wav` only — no `.jps`, no `.mpo`, no `.png`**, so the corpus is invisible to DLNA and the set must be handed `.jpg`/`.mpo`. Still to build: the portal's 3D category browsing the index | `legacy-3d-formats.md`, `tools/bravia_3dcatalog.py`, `tools/bravia_anaglyph.py` |
| Audio: what the sets really accept | **MEASURED 2026-09-18** — three paths, three different capability lists, in `audio-capabilities.md`. **HDMI** (from the sets' own ELD, read while the HX855 was the workstation's monitor): LPCM 2ch at 32/44.1/**48 kHz** in 16/20/**24-bit**, AC-3 6ch ≤640 kbps, nothing else; the driver agrees (`RATE: [32000 48000]`), so **48 kHz is the ceiling, measured twice**. **DLNA**: `audio/L16` (16-bit by definition), MP3, WMA, plus AAC on the HX855 only — no FLAC, **no 24-bit path over the network at all**. **Inside MP4 over DLNA, the sets do more**: AC-3 5.1/2.0 → "Dolby Digital", **E-AC3 5.1 and 7.1 → "Dolby Digital Plus"**, AAC 5.1 plays, **DTS silent on both sets**, TrueHD cannot even be muxed into MP4. Owner's library sampled: ~1 file in 5 is hi-res (24/96, some 24/192), and **Serviio downconverts it twice** (depth and rate) because L16 is all the renderer takes | `audio-capabilities.md` |
| Serviio Surround profile | **BUILT + LIVE 2026-09-18** — `user-profiles-3d.xml` now ships two profiles: `sony2011x` (unchanged, everything normalised to AC-3 in TS) and **`sony2011xs` "3D Enhanced, Surround"**, which serves **MP4 + h264 ≤L4.1 + {AC-3, E-AC3, AAC} native** so the original Dolby arrives untouched, transcoding only what the set cannot decode in MP4 (DTS, TrueHD, FLAC, MP3, LPCM), now at **640 kbps** (the ELD's declared max, was 384). Both TVs reassigned via the console REST; **owner-verified: E-AC3 clips replay through Serviio showing "Dolby Digital Plus", and the log shows no transcoding session** — native delivery. MKV cannot benefit: Serviio has **no `mp4` target container**, so Matroska must still go to TS, which is why the lossless MKV→MP4 conversion is the answer for the 3D library | `serviio-3d-explainer.md`, `tools/serviio/user-profiles-3d.xml` |
| Kodi: second server + upstream | **DONE 2026-09-18** — the 3D fix reproduced through a different maker's server (Kodi's UPnP, never transcodes, byte-exact): SEI-only MP4 → 3D engages; same file minus 791 bytes of SEI → flat; MKV → not listed. Harness committed (`tools/kodi-dlna-test/`) with the three traps it cost (`--init`, a free X display, neutral filenames). Upstream: [xbmc/xbmc#29337](https://github.com/xbmc/xbmc/issues/29337) — **our first claim was wrong** (Kodi *does* read the SEI, via FFmpeg frame metadata; caught by running it, see memory `verify-by-running-before-filing`) and was corrected in place into the real finding: every MP4 advertised as `MPEG4_P2_SP_AAC`, wrong about video **and** audio codec | `3d-signalling-ecosystem.md`, `tools/kodi-dlna-test/` |
| UMS: issue became code | **MERGED 2026-09-19** (`bd032ab0f8e6`, by SubJunk; CI green on Lint, Linux 22.04/24.04, Windows, macOS ARM/Intel) — the campaign's **second merge**, after HandBrake. Filed at the maintainer's request — SubJunk asked "do you have any interest in providing code for these improvements?" on [#6329](https://github.com/UniversalMediaServer/UniversalMediaServer/issues/6329), so it was converted into [PR #6330](https://github.com/UniversalMediaServer/UniversalMediaServer/pull/6330) the same day, carrying **both** halves: `-x264-params frame-packing=N` on libx264 transcodes whose output is frame-packed (Output3DFormat-aware, anaglyph/2D skipped, defers to CustomFFmpegOptions), and the Bravia profiles for the generation (`EX725`, `HX`, `HX75`) — the **EX725 profile had no `f:mp4` line at all**, so every MP4 was transcoded on a set that plays it directly, and E-AC3 is now declared for MP4 (decoded to 7.1, shown as "Dolby Digital Plus"). #6329 answered in the owner's words and closed onto the PR; two triage comments sweep 14 search terms and conclude honestly that **only #6329 is closed by it**, with #1775/#5974 flagged as same-family-different-mechanism and #6099 ruled out (2017 Android-era set, v15 regression). Limits stated in the PR, not left for review: **no local build** (no Maven, Java 25 vs the project's 17) so the Java change has a syntax check only, and the profile edits stop at the measured chassis generations | `docs/upstream/media-stack/README.md` |
| Player/framework survey + Media3 + VLC | **2026-09-19** — every open project in the chain read from source (`docs/3d-signalling-ecosystem.md`). **GStreamer and MPC-BE already do it correctly** (GStreamer's `h264parse`→caps→`x264enc` auto-write loop is the reference to cite everywhere else). **VLC has a measured gap**: 3.0.23 decodes the SEI and renders stereo from it, then drops it on re-encode unless `--sout-x264-frame-packing` is passed by hand; its chromecast module never sets it either (though the cast path cannot carry automatic 3D regardless — the device decodes to HDMI). **Media3/ExoPlayer never parses the SEI at all**; filed [androidx/media#3419](https://github.com/androidx/media/issues/3419), reviving [ExoPlayer#7869](https://github.com/google/ExoPlayer/issues/7869) whose 2020 rejection was literally "you are the first one to ask" — owner will sign Google's CLA if they take the patch. **Triaged 2026-09-22, 40 min apart:** labeled `needs triage` (10:48:59Z) then maintainer **microkatz self-assigned** (11:28:34Z, verified via timeline API) — the first maintainer contact on a lane whose 2020 answer made demand the deciding factor **Chromecast is not contributable** (closed receiver/SDK; Cast docs never mention 3D). **VLC goes to the owner to submit**: VideoLAN bans AI-generated contributions in its GSoC programme (wider scope unconfirmed, wiki 502). **2026-09-22: account approved (Felix, vlc-devel), patch re-verified against master after the mkvtoolnix/mpv rounds (applies 1:1, zero fuzz; container-wins guard h264.c:1219-1243, avcodec map video.c:1261, transcode fmt_in copy encoder/encoder.c:91 — all unchanged), fork created from here on the owner's go, branch pushed as `0f3d63a`, and MR [!10366](https://code.videolan.org/videolan/vlc/-/merge_requests/10366) opened the same day** — description discloses AI assistance honestly, per doctrine. **2026-09-23:** CI green and mergeable, in review; two VideoLAN developers objected to the volume of AI-assisted text rather than to the code, both answered on 2026-09-22, quiet since. Waiting for a technical review; no more comments unless asked. **Round 3, 2026-09-23:** Steve Lhomme reviewed the code and found two real problems (frame alternation can pair the wrong eyes after a dropped frame, because x264 labels eyes by frame parity; cross-project links auto-linked wrongly on GitLab). Both fixed the same day: `55a0dd7` maps only the spatial layouts, and the description is now 168 words with full URLs. Daniel answered all three reviewers himself, in Portuguese or Google-translated; Thomas Guillem set the working rule (write in Portuguese, Google-translate to English), and Marvin Scholz translated Daniel's note for the others. Alexandre Janniaux's claim that `ai-skill/SKILL.md` was Daniel's prompt is answered by its date (created after his MR messages) and its rewritten rule 15. Round 4 (Steve: keep -1's meaning, no forward declaration, no needless rebase) was handled in one push (`70dd7fa`, new default -2 "Same as input"). **Later the same day VideoLAN blocked the `capitain_jack` account and relabelled the MR `AI::Slop` / `MRStatus::NotCompliant`** (**checked again the evening of 2026-09-23:** the MR now shows `AI::Generated` + `MRStatus::NotCompliant`, still open; the public API lists `AI::Slop` on no VLC MR and `AI::Generated` on this MR only, while the MR's last update is still 13:41:11 UTC; a label change on the MR would have moved that time, so the label was most likely renamed project-wide from `AI::Slop` to `AI::Generated`; inference, since the label history needs a login). **Daniel contacted Jean-Baptiste Kempf (VideoLAN president) directly on LinkedIn, 2026-09-23 19:52 -03**, with a connection request and this message, verbatim: "Big fan of VLC for years, my player of choice and the one I recommend." / "Hi! Just wanted to connect and say thank you for VLC being so awesome! I have some upcoming projects that will need 3D SEI signal to be present at the decoder, when will VLC adopt the spec for x264 properly on this?". **PROVENANCE updated in all five repos (2026-09-23, "What the record shows"):** measured on VLC's public API, 297 of its last 300 merged MRs (99%) received human review comments before merging, the other three being release fast-tracks by core developers; our record in the same weeks: HandBrake #8100 and UMS #6330 merged as submitted, MKVToolNix !6311 after review, mpv #18490 after the lead maintainer's review and CI; plus the nouveau course correction and VLC's `AI::Slop` label (now `AI::Generated`). Commits: sony-bravia-linux `e7a3e3a`, awesome-stereoscopy `b697b89`, awesome-linux-hdr `eaf87a8`, awesome-vr `55b0ac4`, awesome-ar `bd5ce45`. **VDD26 (VideoLAN Dev Days, Rome, 16-18 October 2026):** Daniel wrote to the organisers (VideoLAN's board) on 2026-09-23, proposing a talk, "The 3D signal nobody passes on: the H.264 frame-packing SEI across x264, FFmpeg, mpv, VLC and Kodi", and asking whether it can be given remotely or with travel support; call-for-talks form deadline 1 October 2026. **VLC's open-MR picture, measured 2026-09-24 00:00 UTC on the public API:** 521 open MRs (73 drafts); by time since last activity 66 under 7 days, 80 from 7 to 30 days, 245 from 30 to 180 days, 51 from 180 days to a year, 79 over a year (72% idle over a month); labels include 185 `MRStatus::NotCompliant`, 135 `MRStatus::Stale`, 95 `MRStatus::InReview` (29 of them idle over 30 days). 26 open MRs (5%) carry an AI label: 25 `AI::Assisted`, including MRs by VLC core developers (Thomas Guillem, Felix Paul Kühne, François Cartegnie), mostly in ordinary review states; and one `AI::Generated`, !10366, the only MR in the project with that label. The backlog is not an AI phenomenon. **VLC's main branch already contains declared AI-assisted commits** (GitHub mirror commit search, 2026-09-24): four merged 2026-08-31 to 2026-09-04 with `Assisted-by: claude-code:claude-opus-5-0` (`36670139c8`, `0bc4ad4a0c`, `2dd6898314`, `c0a6eb3d96`), and one merged 2026-04-22 with `Co-Authored-By: Claude Sonnet 4.6` (`bd7d103792`, a +208/-48 refactor across 18 files). So declared, reviewed AI assistance is accepted at VLC; !10366 declared its assistance the same way. **Labels cross-compared (GitLab commit-to-MR lookup, 2026-09-24):** the `Co-Authored-By: Claude Sonnet 4.6` commit came in through !9012 (merged 2026-04-22) with no AI label; the `Assisted-by: claude-code:claude-opus-5-0` commits came in through !10096 and !9989 (merged 2026-09-08 and 09-15) labelled `AI::Assisted`; `AI::Assisted` was already on MRs created the week of !9012; `AI::Generated` has been used on one MR only, !10366. **awesome-stereoscopy now recommends mpv** (2026-09-24, `41d2a5a`): "Our pick: mpv", because its upstream code detects the stream-signalled 3D layout (PR #18490) with no fork needed; until mpv's next release (the latest, v0.41.0 of 2025-12-21, predates the merge) that means a development build from 2026-09-23 on; our VLC fork stays listed for VLC users; mpv is also stated as the maintainer's personal choice. mpv is kept out of the VLC dispute: never named in anything addressed to VideoLAN. **Daniel's position on AI and credit (2026-09-24, his words):** "My usage of AI can be AI assisted and/or Co-authored, I, differently from them, do not care who, I do care that the end result is public and world wide." The work is judged by the result that reaches people, not by who gets the merit; in his view the AI shares that indifference to credit, the same afternoon mpv merged the same principle through its lead maintainer's review. The MR stays open with the fixed patch; no workaround to the block; the Code of Conduct email to the VideoLAN board is the channel (support ticket #8187), and the public trail records the rest. **2026-09-24:** the account was unblocked "for now" (Daniel had not asked; after the block he sent only the CoC concern); he replied the same day with the facts and the full list of his API calls, board and Jean-Baptiste Kempf in open copy (correspondence kept privately). Jean-Baptiste answered the VDD26 proposal personally: VideoLAN prefers no remote talks at the Dev Days. Stance until the board answers: nothing on !10366, no contact with VideoLAN's servers. The same evening VideoLAN released VLC 3.0.24, whose announcement says many of its 130+ security fixes were "found with the help of LLMs". **Then the FOSS thing: [fork](https://github.com/danielcamposramos/vlc) and [release 3.0.24-3d1](https://github.com/danielcamposramos/vlc/releases/tag/3.0.24-3d1).** Testing the builds found the real root cause: VLC's `HxxxParseSEI()` parsed the frame-packing SEI but never passed it on (bug since 89347c3e2d, July 2017, in every VLC since 3.0.0, still on master), so the x264 patch alone could never work. With the one-line fix, all four stock-vs-patched tests pass (Debian 13 raw and MKV, Debian 12, Windows under Wine). Released: Windows installer and portable, Debian 13 and 12 packages, patches and checksums, every asset re-downloaded and verified; listed in awesome-stereoscopy. **VLC-HiFi and VLC-3D-HiFi for Android PUBLISHED 2026-09-23:** fork [danielcamposramos/vlc-android](https://github.com/danielcamposramos/vlc-android) (branch `3.7.1-hifi`, with the build script and patches) and release [3.7.1-hifi.1](https://github.com/danielcamposramos/vlc-android/releases/tag/3.7.1-hifi.1); see the Android row below. Planned last: offer the work back to VideoLAN. Details in the tracker, rows 6 and 16 | `docs/3d-signalling-ecosystem.md`, `docs/upstream/media-stack/README.md` |
| VLC-HiFi / VLC-3D-HiFi (Android) + ROM 3D contract | **PUBLISHED 2026-09-23.** VLC for Android 3.7.1 with VideoLAN's engine plus our patches: float output for mono/stereo, SoXR VHQ by default, configurable buffer; VLC-3D-HiFi adds the three 3D patches. Own app IDs (`ai.echosystems.vlc.hifi`, `.hifi3d`), EchoSystems signing key (the 2026-08-01 key, cert `7a8367d1…`), EchoSystems About card; VLC's fixed-name extension permissions renamed so it installs beside any VLC. **Measured on two boxes, same 24/96 file, stock VLC as control:** stock cuts to 16-bit inside VLC and resamples at "Medium 16-bit", handing Android a PCM_16_BIT track; VLC-HiFi keeps float, resamples at "Very high quality" and hands Android PCM_FLOAT; TVE T10 (Android 10) and RK322x (2015 RK3229, Android 8.1) both on our 24-bit HAL ROMs (PCM_8_24 / S24_LE 48 kHz); 0 new underruns in steady state on both; Daniel heard it through the Sony. **The ROM caps the rest:** stock HALs are 44.1 kHz / 16-bit though the hardware is capable, which the release notes state openly ("Yes, Android caps the audio. Here is exactly where."). **3D:** full-screen SEI playback leaves HDMI in 2D on the T10 (Allwinner stack does not turn the SEI into the InfoFrame); both boxes switch through our ROM watchers (`[3D]` title, `org.videolan.vlc` only); a Chromecast receives the SEI and still outputs 2D (measured). Design: VLC-3D-HiFi asks the ROM through a signature-permission service, watcher extended to SEI/MKV/extension, plus a manual switch; the Gadmei T883-3D (Amlogic, glasses-free) shipped the same two-path design (its players drive `enable3d`/`ppmgr`, its Activator writes `enable-3d` and `twx3d/lense`). ROMs move from echo.1 to VLC-3D-HiFi; the bake (with GPT) updates every integration naming `org.videolan.vlc`. Planned: a Consumer Rights Wiki article on audio capability gating on capable Android devices. Not yet done: a clean-clone rebuild with the published script | `docs/upstream/issue-tracker.md` row 16, `docs/outreach/vlc-hifi-3d-audience.md`, sparky-armbian `05_board_tve_t10/reports/` (`VLC_3D_ROM_CONTRACT_DESIGN_20260923.md`, `GADMEI_T883_3D_STUDY_20260923.md`) and the handoff `app-staging/candidates/current/vlc-echosystems-public-3.7.1-20260923/` |
| Campaign follow-up round | **POSTED 2026-09-18** — seven live threads. From this session (not gated): Gerbera #3937, UMS #6329, Jellyfin PR #18060, repo Discussion #1. By the owner in his own words (gated targets): mpv PR #18490, FFmpeg #24531, Codeberg mkvtoolnix #6309. All email-trigger watch, never polled | `docs/upstream/media-stack/README.md` |
| Consumer Rights Wiki, reviewer follow-up | **2026-09-19, the pt-BR argument was adopted** — after the talk-page reply, another editor created the missing CS1 language categories including Brazilian Portuguese and Russian, and reviewer **Sojourna** switched the article's eight Portuguese citations from `pt` to `pt-br` and replaced all 28 `&mdash;` entities with the symbol. The live wikitext is saved as the new baseline (`wiki/consumerrights-wiki-entry-live-2026-09-19.txt`): future edits must start from it, never from our local copies, or they would revert another editor's work. Update 9 changed one line against that baseline, tagging the Russian source `lang=ru`; **POSTED 2026-09-19 by the owner in the browser.** The `pt-br` category page was created, but `Category:CS1 русский-language sources (ru)` still does not exist, so it renders red for now. Left on purpose as early adoption: the tag is correct, and the link turns blue by itself when anyone creates the category page, with no further edit to the article. **Resolved the same evening:** Sojourna noticed the red category and created it at 20:51 UTC as a `__HIDDENCAT__` maintenance category, so the early-adoption call worked in minutes and the article needed no further edit. Thank-you note posted to Sojourna on the talk page | `wiki/consumerrights-wiki-entry-2026-09-update-9.txt` |
| Consumer Rights Wiki, update 8 | **POSTED 2026-09-19** — citation language codes restored as ISO 639 codes after reviewer **Sojourna** answered the talk-page question (MediaWiki does not distinguish pt-BR from pt for CS1). Measured against the wiki's own category list before editing: `pt`, `de`, `it`, `es`, `ja` category pages **exist**; `pt-br` and `ru` **do not** — which is exactly what produced the red-link category in update 6. Ten citations tagged (8 pt, 1 de, 1 it); the Russian source left untagged rather than red-link it. Talk-page reply asks the maintainers to create both missing categories, arguing pt-BR on substance (the module already renders the *português do Brasil* autonym, so the code is supported; and on a consumer-rights wiki a Sony Brasil page and a Sony Portugal page are different products, policies and legal regimes) | `wiki/consumerrights-wiki-entry-2026-09-update-8.txt` |
| Consumer Rights Wiki, update 7 | **POSTED 2026-09-19** — the 3D incidents rewritten from the new measurements. The still-photo incident is **corrected, not extended**: MPO from USB works on both sets, so the old "no stereoscopic option" claim was wrong; what is true is narrower and worse — the sets accept **only** MPO, **only** from USB, and show every other valid stereo photograph (JPS from a 3D phone, side-by-side JPEG, community pairs) flat, with no message explaining why. Adds the set's own on-screen **"Sinal 3D foi detectado"** notification to the internet/app incident plus the USB contrast, the four-variant probe result (items the set never declared are hidden), and Brazilian price context — minimum wage cited to Decreto 7.655/2011, the television's price deliberately **approximate** (owner's recollection and circulating figures disagree; archive.org was offline for the retail captures) | `wiki/consumerrights-wiki-entry-2026-09-update-7.txt` |
| Consumer Rights Wiki, update 6 | **POSTED 2026-09-18, cleanup notice answered** — reviewer flagged "some citations not formatted"; the article was rebuilt to the `ProductPreload` outline with all 37 refs converted to `{{Cite web}}` (36 templates), Sony's source-code obligation wrapped in `{{Quote}}`, incident headings dated, "Products" → "Affected models", and the firmware incident narrowed further (the support route is untested — stated as such). Two traps found by measurement: the paste path **breaks source lines at 4096 chars** mid-token (that, not a bad paste, caused the first round of "CS1 errors"), and CS1's `|language=` / bare `|url-status=` add tracking categories **that do not exist on this wiki** and render as red links — both removed, language moved into the visible note. Final categories: only the two from `{{Incomplete}}` | `wiki/consumerrights-wiki-entry-2026-09-update-6.txt` |
| Consumer Rights Wiki, update 3 | **POSTED 2026-09-18** — the whole article pasted in source mode, built from the live page. Adds the **3D still-photograph incident** (five archived Sony sources contradicting each other across regions) and strengthens the DLNA 3D incident with the independent second-server reproduction; eleven-language docs added to See also | `wiki/consumerrights-wiki-entry-2026-09-update-3.txt` |
| awesome-stereoscopy | **CREATED 2026-09-18** — [the field had no curated list](https://github.com/danielcamposramos/awesome-stereoscopy); searches under every wording returned nothing. 200+ verified entries organised by one argument: 3D means depth, and depth is what two eyes are for. Prehistory (Mohist camera obscura, Euclid, Ibn al-Haytham, Sushruta), Victorian stereoscopes, Hollywood and the theme parks where 3D never stopped, anaglyph colour codes, the glasses-free phones, headsets as worn stereoscopes, depth capture and 2D-to-3D conversion, generated views, and an explicit **known-gaps** section. Cross-linked with this repo; 2026-09-22: `standards.md` gained the HDR-over-HDMI normative chain (HDMI Forum 2.0a, CTA-861.3-A with the official free CTA preview, BT.2100 + pinned BT.2100-3, DRM uapi definition) that the #1384 filing cites | — |
| Contribution lane + co-maintainers | **OPEN 2026-09-23** across this repo and the four lists (awesome-stereoscopy, awesome-vr, awesome-ar, awesome-linux-hdr). Each repo has a PR template (hand-checked links, one entry per line, AI-assistance disclosure pointing to PROVENANCE's *On slop* section), issue forms (dead link; suggest an entry on the lists; test report from your TV here), CODEOWNERS, a PR check that fails only on awesome-lint errors above the base branch plus lychee on changed files, and a weekly link scan that opens an issue (403/429 accepted as bot-walls for a human to check). `main` is protected everywhere: PR + code-owner review (+ `lint` on the lists), admins exempt. Invitations posted and pinned as issue #1 on each list, plus an update on Discussion #1 here. **First contributor 2026-09-23:** udaya-ann (OSCI'26 student participant, account from 2024) offered HDR driver/property documentation on awesome-linux-hdr#1; the owner answered with a one-driver first scope. Every PROVENANCE.md now carries the *On slop* section, linked to the owner's CR Wiki talk-page comments and citing Linus Torvalds' AI-assisted drm/xe commit 818bebeb63dd (read via the GitHub mirror; git.kernel.org refuses automated fetches). **AI skills 2026-09-23:** each repo has `ai-skill/SKILL.md` + README in the crwiki-ai-skill shape (senior-partner posture, the repo's own rules, how to meet them, the incidents behind each rule), linked from On slop, contributing.md and the PR template; public text names Daniel, not "the owner". Offered to udaya-ann on awesome-linux-hdr#1. **First contribution 2026-09-23:** udaya-ann opened [awesome-linux-hdr PR #2](https://github.com/danielcamposramos/awesome-linux-hdr/pull/2) (amdgpu DC HDR properties and infoframe path, pinned to mainline source, ChatGPT disclosed); verified line by line, checks pass, approved as is by Daniel with three optional improvements offered as encouragement; left open for the contributor. **First external link to our lists:** szenergy/awesome-lidar#26 added awesome-ar to its related lists (merged 2026-09-23). **Fork fallback** stated in `docs/upstream/community-hdmi-3d-patches.md` (upstream first; the MIT-licensed kernel-open modules may be carried per driver release if a right fix never lands) | `.github/` in each repo; `docs/upstream/issue-tracker.md` row 14 |

## Rules of engagement (hard constraints)

These apply to any partner working on this project:

1. **Own hardware, own LAN only.** Both TVs, the TV boxes and the
   server are the owner's property on his LAN.
2. **No DRM circumvention.** PlayReady/WMDRM/Marlin/CI+ are out of
   scope. We serve the owner's own media to the owner's own sets.
3. **Never open the TVs.** Non-invasive lanes only (unless the owner
   explicitly drives a UART adventure — see the roadmap's gates).
4. **Never port-scan the TVs.** Ever. We know their addresses; we
   talk to documented endpoints only.
5. **EX725 (.22) first, always.** Never crash-test the HX855 (.21) —
   it is the workstation's monitor. Changes land on .22, get
   owner-verified, and only then touch .21.
6. **No firmware/update host is ever pointed at us.** The DNS-override
   list on d2server's Unbound is a **closed, owner-approved set of
   exactly two hosts**: `applicast.ga.sony.net` → 192.168.0.60 and
   `rd1.sony.net` → 192.168.0.60. Nothing else is ever overridden;
   adding a host means the owner says so first. `ssm`/`ssm1`/
   `playstation`/`static.internet.sony.tv` are never MITM'd and never
   overridden.
7. **Heavy media jobs run on d2server**, never the workstation
   (ffmpeg/mkvmerge/SEI batches go to .60 over SSH).
8. **Kill and start are two separate SSH calls** (see above).
9. **Nothing Sony-copyrighted enters this repo** — firmware images,
   widget packages, third-party app sources stay in the owner's
   offline archive. See below.
9b. **The preservation archive stays private; distribution is not ours
   to decide.** The AppliCast/BRAVIA material recovered from Sony's
   still-live CDNs (2026-09-17: three hosts, the widget bundles, their
   catalogs and assets) is held offline by the owner. The plan is to
   **share it with the right-to-repair movement** — and if they judge
   that it should be distributed, that call is theirs, not this
   project's. What this repo publishes is the analysis, the paths, the
   method and our own reconstructions; never the recovered bundles.
   Contributing the map back to the Internet Archive is a separate
   decision on the same terms.
10. **Check for live streams before restarting a serving service.**
10b. **When a reviewer gates on the AI assistance instead of the diff, ask the project's lead or responsible maintainer for a technical review of the diff.** Politely, once, and in the thread. That is how mpv #18490 moved: Daniel asked kasper93 for a neutral technical review, kasper93 reviewed the code, the changes he asked for were made, and he took the PR over for landing.
11. **Pre-publish sweep (hard):** before publishing this repo or any
   part of it anywhere (repair.wiki, a fork, a tarball), sweep for
   key/credential material: `git grep -l "BEGIN.*KEY" $(git rev-list
   --all)`, check no `*.key` or `creds.json` is tracked, and confirm
   the .gitignore tripwires are intact (firmware/, manuals/, widget
   trees, creds.json, `tools/rd1-portal/*.key`). Key material lives
   only in the owner's private archive and on the server.

## Private material

A sibling folder (`sony-bravia-linux.private`, offline, not in this
repo — ask the owner) holds: the Sony-distributed archives (firmware
images, the era widget bundles fetched from Sony's dead servers),
third-party copyrighted app sources, and credentials/key material.
The **public/private split rule**: research notes, protocol
knowledge, our own code, and anything needed to *operate* the stack
are public; Sony-distributed material, credentials, and anything
that identifies other parties' copyrighted code are private. LAN
addresses appear in this repo by the owner's choice (RFC1918,
unroutable, needed for the docs to make sense).

## What's next (pick-up menu)

**Media stack polish** (low risk, high value):
- thumbnails on list pages (poster art is already in the players; the
  list pages still have none)
- audio-track selection for direct-playable MP4s
- receiver pass-through test (HDMI 5.1 advertised, no AVR on the
  verified setup yet)
- album art on top of the music list in album view (instead of
  beside it)
- cookie power-cycle survival check on the set (dated cookies
  persisted across a browser relaunch; a full AC power cycle is
  unconfirmed)

**Closed out of this menu 2026-09-18:** auto-advance/repeat on `ended`
(now built — cookie-persisted repeat, auto-advance to next; the reload
flash is acceptable inside the native-controls player) and the real
key harvest (done on the EX725; the answer is that the media keys
produce no keydowns at all — see the key section).

**Verification tests on the sets** (owner's timing, EX725 first):
- the native-DLNA 3D hypothesis: does the TV's *own* DLNA player
  switch 3D for the same SEI-carrying file the browser cannot switch?
  (the browser-input block is documented; the DLNA lane may not be)
- HX855 promotion of the restored widgets (verified on EX725 only so
  far)

**Pending deploys**: (none — tvbox-vlc renderer profile was deployed
and assigned 2026-09-15; verify on the boxes by browsing mpc/wv and
confirming LPCM delivery)

**Kernel upstream — DONE 2026-09-21**: the Tested-by/evidence reply was sent to amd-gfx (cc dri-devel), threaded on the Betschart v3 cover letter, Message-ID <20260921034104.1021664-1-capitain_jack@yahoo.com>. Watch the thread for maintainer replies; never post again without Daniel:

- Subscribed to amd-gfx (capitain_jack@yahoo.com); reply edited in his own words and sent in-thread (git send-email, plain text, links inline; HTML is banned on kernel lists)
- Our mboxes 0001/0002 stay unsent unless maintainers ask or the v3 stalls
- Adrian answered asking for a run of v3 itself (TaB first, so patch 2's Ext_Data byte gets coverage); we built 7.3-rc4 + v3 verbatim (`/K3D/temp/k73-v3/`), booted it one-shot via dpkg, and ran all three layouts in one window 2026-09-21 12:14-12:19 UTC-3: **TaB / SBS / FP all PASS on the KDL-46HX855**, probe numbers identical to the 7.0 port (record: `tools/stereo-modeset/run11-betschart-v3-7.3rc4-all-pass-2026-09-21.log`; harness gained `K3D_STOCK_MODULE=1` and `MODE=all`). Tested-by now covers v3 as posted
- Follow-up reply **SENT 2026-09-21 12:34 UTC-3** (Daniel reviewed/edited, green-lit the send): reports the v3-as-posted all-pass run and offers the prepared 7.0 backport, offer-only framing (his explicit choice); full reply-all to Adrian + the maintainers and lists he Cc'd. Message-ID <20260921153400.53321-1-capitain_jack@yahoo.com>, public in lore in-thread

**NVIDIA-proprietary lane — runs 12-17, 2026-09-21**: 615.71.09 (no swap, no removal) prunes every stereo mode — baseline probe 17 modes / 0 stereo forever (`stereo_allowed` appears nowhere in the open glue). BUT the glue exposes an `NV_HDMI_VSIF_METADATA` connector blob property whose payload crosses into NVKMS with the modeset (nvidia-drm-connector.c:592-600 → NVKMS), so `stereo-modeset ... isolate vsif` does a plain 2D modeset plus a userspace-injected 6-byte HDMI 1.4 3D VSIF. Matrix on the KDL-46HX855, now closed: **SBS-half 1080p60 PASS twice** (runs 13/14) and **TaB 1080p24 FULL PASS** (run 17 — engage + proper picture + working single-eye 2D fallback; bytes byte-identical to the failing injections, `03 0c 00 40 60 00`, so the payload is exonerated); TaB at 60 Hz is the failure class — 1080p60 = whole stream muted incl. the 2D-toggle path (runs 14/15), 720p60 = 2D decodes while 3D stays lit-black (run 16); frame packing refused by design (doubled link timing needs real mode support). Same sink ate identical TaB bytes at 1080p60 from the kernel-driven AMD link (run 11), so the boundary is NVIDIA-delivery-specific: injected TaB lands at 24 Hz, never at 60 Hz, while injected SBS lands at 60 Hz — mechanism opaque without an HDMI analyzer [inferred: NVKMS infoframe cadence/placement at higher line rates], proper fix = native stereo exposures. Endstate: GitHub issue to NVIDIA/open-gpu-kernel-modules carrying the pipe evidence + closed matrix + the minimal glue delta (expose `stereo_allowed`) — **POSTED 2026-09-21 as #1382** under Daniel's account on his explicit order ("push it for me"), patch attached by him in the first comment, PR-offer comment posted; community installable variant live on the awesome-stereoscopy list via `docs/upstream/community-hdmi-3d-patches.md`. Records: `tools/stereo-modeset/run12…run17-*.log`, harness `run-nvidia-test.sh`. **Runs 18–19 closed the lane 2026-09-22: aritger asked for a test case; run18 answered with the measured A/B of stock vs the six-line patch (both 17/0 — proof the gate alone is insufficient, and why); run19 measured the v2 glue-side VSDB synthesis built in this repo (same harness, same boot, same sink): 39/22 stereo under the STEREO_3D cap, none without it. Patch, design doc and log posted to #1382 by the owner ([comment 5771464134](https://github.com/NVIDIA/open-gpu-kernel-modules/issues/1382#issuecomment-5771464134)); open question left with NVIDIA: whether NVKMS's EDID parser models VSDB 3D at all.** **All upstream touches of the campaign are tracked in `docs/upstream/issue-tracker.md`** (whose-move per row, identifiers, watch method; 2026-09-21)

**NVIDIA mode-list issue 1 — ON HOLD (owner, pacing)**: the CTA dual-rate VIC fix is prepared end to end (patch, userspace parse proof 28→40 timings, clean module build, approved issue text) but held so NVIDIA is not flooded while #1382/#1384 answers settle. Before filing, run the bench A/B `tools/stereo-modeset/run-nvidia-cta-dualrate-probe.sh` (same desktop drop as runs 18/19, about a minute; CUDA untouched; predicted 17→23 modes). The kapi stereo-flag PR also waits for aritger's answer on #1382. Details: `docs/upstream/nvidia-cta-dual-rate-issue-draft.md`.

**Stereo gaming (VR Stereo Spectator)** — gamescope frame race fixed 2026-09-25 (seen first as the anaglyph depth swim); next: p20 (motion blur back on, through gamescope), the in-game 3D offer to Valve (PR 1 tested on HL2 q01-q07; next the TF2 bed for the client-side fixes, the remaining #8297 client fixes, PR 2 and PR 3), watch gamescope#2438; the viewmodel attachment fix gated on the display module (no cheat unlock); watch Source-1-Games#8297 and SteamVR-for-Linux#961 (whose move: Valve); record an HL2 RTX demo in HL2 RTX itself (the HL2 demo is refused: `game directories don't match`); run bench sessions B (nouveau + NVK) and C (wiz3D under Proton, the side-by-side comparison); OpenGL in VR mode (crashes in the engine's render thread); the player mode for SteamVR (an OpenVR driver presenting a 3D display) once #961 says which compositor path is accepted.

**Bigger lanes** (see the roadmap for gates and ordering):
- widget lane: run owner-built applicast widgets on the sets
- sony.tvstore.opera.com revival attempt (era Opera store host)
- UART/TFTP boot path confirmation (the unbrickability gate)
- custom kernel + initramfs (Stage 1 of the roadmap, post-gate)

## Document map

| Doc | Content |
|---|---|
| `platform-map.md` | The hardware/software platform in depth |
| `feasibility-roadmap.md` | Staged plan: content → widgets → root → kernel |
| `oss-source-recovery.md` | Sony's 2014 OSS listing **did** include KDL-46EX725 and KDL-46HX855 (archived proof); today it lists 798 KDL models and none from this generation. Recovers the 23-package manifest for our model group and 83 packages KDL-era-wide (incl. the MIPS toolchain); files themselves are 404 live, unarchived, and unmirrored — the kernel survives only in the owner's archive |
| `licence-basis.md` | **Evidence file, not legal advice**: the sets run GPL Linux and Sony shipped the source with its licence; Sony's Source Code Distribution Service is live in 2026 and lists 798 `KDL-` models — **zero** from the 2010–2012 EX/HX/NX/CX generation. Separates what the GPL grants an owner (does not lapse) from the distribution offer (three-year term), and marks the line the widget bundles sit on |
| `right-to-repair.md` | Context + Rossmann/repair.wiki/FULU sharing plan |
| `hdmi-cec-audio-system.md` | **Parked lane, documented**: 26 Sony vendor CEC opcodes + the 102-model audio compatibility table recovered from the AudioControl bundle; why the widget reports unavailable; the untried `device_type=4,5` direction and the zero-risk experiment that would justify it |
| `worldclock-schema-reconstruction.md` | **First reverse-engineered solution**: World Clock's `preference.xml` no longer exists on any Sony server; reconstructed from the widget's own code (Item1=GMT offset, Item2=DST, Item3=AM/PM) and deployed. Includes the reusable method for any dead settings screen |
| `build-your-own-bravia-portal.md` | **The public method guide** — how to give these sets a working portal + media browser on your own LAN with your own hardware: the two (and only two) DNS overrides, era-TLS vhost, the media app, the widget-restore catalog edit, and every measured platform gotcha |
| `era-key-vocabulary.md` | The measured remote/key model: no media-key keydowns, colour keys are Opera's, the glyph coverage table (the whole Arrows block is tofu), the Medium-font caveat |
| `era-media-element.md` | The measured `<video>` element rules: `<source>`+type or nothing loads, never clip an ancestor (silences audio), `-o-transform` scaling + table sizing, `timeupdate` only, dated cookies, the codec canPlayType table behind the transcode lanes |
| `3d-signalling-explainer.md` | The short explainer linked in every upstream post: two signals, which side reads which, the fix |
| `3d-signalling-ecosystem.md` | **The nowhere-else map**: the DVB mandate with honest scope (ETSI TS 101 547-2), the decade of ecosystem symptom threads (Plex/Jellyfin/Serviio/MakeMKV), the cross-brand survey (18/20 videohelp pages, Samsung/LG), recorded-broadcast history (Sky 3D), the fix chain end to end, with timed LTT/Rossmann video citations |
| `3d-blocked-in-browser.md` | The panel detects 3D in browser video but cannot switch (auto or manual) while the same panel switches from other inputs; i-Manual documents the feature with no input restriction; the native-DLNA test that would close it |
| `legacy-3d-formats.md` | **Act three charter** — enable all 3D content ever made: the legacy packings never left the standard (`frame_packing_arrangement_type` 0/1/2/5), conversion to SBS+SEI is the proper path, the anaglyph colorimetry chain, the verified iZ3D sidebar (passive polarized dual-LCD, not glassless — the glasses-free device in the lineage is the Optimus 3D), the YouTube 3D-player withdrawal (the pattern at platform scale, no announcement ever), and the community 3D-photo corpus as first test material (private test use — the photos are the community's) |
| `3d-photos-on-bravia.md` | **3D photos on the set, measured and sourced** — both sets open SBS / half-SBS / anaglyph-derived / MPO-renamed-`.jpg` photos flat over DLNA, and the photo 3D menu offers only 2D→3D (no side-by-side entry for stills); Sony's own support pages contradict each other across DE/RU/IT/BR/JP/UK (archived by the owner, snapshot table inside); Italian owners report MPO stills *did* work from USB on the KDL generation and stopped on Android, and AVForums' 2012 review of the KDL-55HX753 (our board family) states "JPEG over the network, 3D MPO from USB" — **answered 2026-09-19: a spec-correct MPO from USB engages 3D on both sets; over DLNA it stays 2D (HX855 measured), because the network path declares JPEG only — a software gate;** the unmet L/R eye-swap need across ES/BR forums |
| `audio-capabilities.md` | **What these sets accept, audio, by path** — HDMI (from the sets' own ELD: 24-bit LPCM but 48 kHz max), DLNA (L16, 16-bit only, no FLAC), and inside MP4 where they decode Dolby Digital Plus 7.1 natively; why FLAC cannot be "encoded to 24-bit AC-3" (lossy codecs have no bit depth and stop at 48 kHz); and the practical recipe for a hi-res library |
| `i18n/*.md` | **The project in eleven languages** (ja, zh-TW, ko, de, fr, it, es, pt-BR, ru, pl, nl) — the problem and the fix phrased for owners searching in their own words, with the product names; linked from the top of the README. Translated 2026-09-18, native-speaker corrections invited; keep the claims in step with the English docs |
| `judging-by-the-cover.md` | The AI-policy-landscape record: mpv, mkvtoolnix, HandBrake, Codeberg all gate on owned/understood/reviewed, never on "was AI used"; the 42-line standard-based patch as the case study — the "não julgue pela capa" campaign's proof |
| `legal-eula-analysis.md` | Three pillars from Sony's own documents: GPL source (non-disclaimable), 3D display as a function not a "Service", services discontinuation expressly reserved — consumer-rights framing, not a breach claim |
| `regional-documentation-asymmetry.md` | The same product, "different" by region: US i-Manual/EULA/warranty vs BR marketing tips; the CDC framework (art. 18/26/30/32/51), flagged not-legal-advice |
| `sony-end-of-service-statements.md` | Sony's five dated end-of-service notices (Facebook, Skype, SideView, TrackID, Twitter), each naming both models, plus the firmware-removal page — all archived at the Internet Archive with regional copies (Canada/LatAm) |
| `trackid-bgmsearch-recovered.md` | TrackID/BGMSearch: what the service was, the Gracenote SMRP path mapped from Wayback, why the buttons emit zero packets (firmware-local tombstone), and the deferred older-firmware path |
| `manuals-index.md` | Where Sony's own documents live (RefLib doc numbers + links, hosting nothing) and the **provenance split** that decides what may be shared: four Sony-published documents vs. six third-party service manuals, with SHA-256s and the coverage proof that one AZ2-F manual spans 32/40/46/55" |
| `generation-model-map.md` | Which TVs this applies to: AZ1/AZ2/AZ3 generations, service-manual model coverage (32–60" EX725 on one chassis), tier differences (subwoofer/anti-glare/Opera Store), the AZ1 harvest (one widget, dated 2010-04-21; `PAC2.0` XMB plugin; GPSPhotoWidget), and the **check-the-archive-before-declaring-dead** rule |
| `withheld-by-catalog.md` | **The evidence document**: Sony localized the widgets Brazil never got — Portuguese complete, Rio de Janeiro in the city list — and withheld them at the catalog layer. Plus the 2026-09-17 preservation sweep (3 live hosts, 137/168 URLs) |
| `3d-origin-story.md` | Author's 3D history: the 2011 iZ3D license gift (Vadim Asadov) → the SEI campaign; the hologram line that seeded Knowledge3D; the yt3d tip to Joe Penna, dated since 2026-09-25 by Daniel's own email of 24 January 2011. Corrected 2026-09-25: iZ3D's driver is not abandonware (MIT source at bo3b/iZ3D, carried on by wiz3D), the page and its arc are in date order (the Joe Penna section now comes first, then the iZ3D licence; the tip, "3D Test" 30 Dec 2010 and "3D Maestro" 1 Feb 2011 come before the iZ3D licence of 2 Feb 2011), fifteen years not twenty, and no "teenager" or "kid" |
| `rossmann-outreach.md` | Outreach draft to Louis Rossmann, grounded in his own live Sony campaign: quotes pinned to source (Parker Hartline, ASA, the 2023 House hearing — verified, no lawsuit claim), the five Sony videos with timed jump-links |
| `partner-review.md` | 3-partner senior review of the roadmap (2026-09-13) |
| `wiki/` | repair.wiki + consumerrights.wiki page drafts (public versions) — incl. the 2026-09-18 wiki update as posted: five archived Sony notices, the browser-3D incident, the Brazil CDC section, the corrected firmware claim |
| `research/` | Live recon, protocols, CVE notes, kernel survey |
| `research/liverecon/` | Wire captures + analysis from the live sets |
| `tools/serviio/` | Serviio profiles, 3D fix, forum posts, app |
| `tools/serviio/tv-mediabrowser/README.md` | The media app deep-doc |
| `tools/systemd/README.md` | Deploy + service-migration procedure |
| `wiki/` | repair.wiki page drafts (public versions) |

*Status doc written 2026-09-15, updated 2026-09-17, 2026-09-18 and 2026-09-22. Keep
it current: when a lane changes state, update this file in the same
commit.*
