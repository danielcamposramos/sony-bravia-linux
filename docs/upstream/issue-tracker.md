# Upstream issue tracker — every filed/planned touch of the campaign

One row per upstream item. "Whose move" is always explicit. Rules in force for every
row: no agent ever posts to any forge (Daniel posts everything himself); polling is
gentle and content-checked, never past anti-bot controls; email-first where the forge
mails us; never necro closed issues. Sorted by activity, not importance.

| # | item | where | identifier | status (2026-09-21) | whose move | how we watch |
|---|---|---|---|---|---|---|
| 1 | NVIDIA HDMI 1.4 3D: `stereo_allowed` glue delta | github.com/NVIDIA/open-gpu-kernel-modules (issues) | **#1382** (github.com/NVIDIA/open-gpu-kernel-modules/issues/1382) + PR-offer comment 5764542206 | **LIVE EXCHANGE.** Original post + PR offer 2026-09-21 (comment 5764542206); aritger asked for a test case 2026-09-22 01:19 UTC; measured v1 A/B posted (comment 5770907110 — stock 17/0, stereo_allowed-patched 17/0, necessary-but-not-sufficient); color-depth/TaB PS (comment 5771032927); **v2 VSDB-synthesis patch + measured numbers posted 2026-09-22T05:03Z ([comment 5771464134](https://github.com/NVIDIA/open-gpu-kernel-modules/issues/1382#issuecomment-5771464134), 3 attachments: v2 patch, design doc, run19 log — 39/22 stereo under STEREO_3D cap, gap to nouveau = missing 1080i family in NVKMS base list)**; open question to maintainers: does NVKMS's EDID parse model VSDB 3D at all; **answered by aritger 2026-09-23T00:47Z (yes: `nvt_edidext_861.c`, "ThreeD"), who asked how the stereo mode would be used; owner answered with use cases the same evening; 2026-09-23 follow-up ([comment 5787596957](https://github.com/NVIDIA/open-gpu-kernel-modules/issues/1382#issuecomment-5787596957)): test case = `tools/stereo-modeset/stereo-modeset.c`; real gap = `NvKmsKapiDisplayMode` has no stereo field, so NVKMS's `hdmi3DAvailable` (frame packing only) never reaches nvidia-drm; proposed kapi-flag PR, asked if that is the wanted direction** | **aritger** | one gentle `gh api repos/NVIDIA/open-gpu-kernel-modules/issues/1382` check on request; no crawling |
| 2 | Betschart v3 HDMI 1.4 3D series (amd-gfx) | amd-gfx list, lore.kernel.org/amd-gfx | Tested-by mail <20260921034104.1021664-1-capitain_jack@yahoo.com>; v3 PASS + backport offer <20260921153400.53321-1-capitain_jack@yahoo.com> | BOTH SENT, public in lore in-thread (verified 2026-09-22 via lore t.mbox); Tested-by covers v3 as posted (runs on 7.3-rc4+v3, all three layouts); **Adrian acked OFF-LIST 2026-09-22 09:53 +0200 (private reply, not on the list): thanks for running v3 exactly as posted and TaB-first, a Tested-by covering the series as it sits "is worth a lot", and the 7.0 backport readiness is welcomed for older kernels — no asks** | maintainers / Adrian | lore thread check on request; amd-gfx also reaches Daniel's subscribed inbox; Adrian may also write privately |
| 12 | NVIDIA unified EDID-surface issue (3D absent + 10v12 deep colour + 17/51 prune; HDR adjacency; cites #1348, #1369, #1184, HDR trio, #1101) | github.com/NVIDIA/open-gpu-kernel-modules (issues) | **#1384** (github.com/NVIDIA/open-gpu-kernel-modules/issues/1384); draft at `nvidia-edid-surface-issue-draft.md`; cross-link from #1382 = comment 5771657559 | **FILED 2026-09-22** by gh as danielcamposramos on explicit per-act approval; machine line: RTX 3060 (GA106), GALAX, 12 Gb VRAM; asks NVKMS-vs-glue ownership + EDID capability model; **aritger replied 2026-09-23T01:15Z: both nvkms and the glue are open to community changes, placement depends on each bug, explanations must be succinct, start from `NVT_EDID_INFO`, keep HDR separate. Owner replied 2026-09-23 ([comment 5787595679](https://github.com/NVIDIA/open-gpu-kernel-modules/issues/1384#issuecomment-5787595679)): the body's claim that NVKMS parses none of the VSDB 3D data was wrong, corrected; 3D gap continued on #1382; HDR kept separate; mode-list bugs to be filed as small single-bug issues (pending)**; **same evening: the deep-colour third is now *proven* on this exact stack — `max_output_color_depth=12` param reload drove the DC_36 sink at 12 bpc (run24 PASS log committed), so the staged default-cap patch (bbfc670) is measured-sufficient; **PR FILED 2026-09-22 evening: [#1386](https://github.com/NVIDIA/open-gpu-kernel-modules/pull/1386)** (one-liner, Refs-not-Fixes #1384, cites the nouveau list series as open-source counterpart + the run24 log; **title retitled same evening per owner passes: "nvidia-modeset: raise default output color depth for HDMI 1.4 Deep Color, EDID VSDB DC_30/DC_36/DC_48"**) | **NVIDIA maintainers** | GitHub email notifications only (author is Daniel) |
| 13 | NVIDIA mode-list issue 1: CTA VICs only at the 1000/1001 rate (24.000/30.000/60.000 Hz twins missing) | github.com/NVIDIA/open-gpu-kernel-modules (issues) | draft `docs/upstream/nvidia-cta-dual-rate-issue-draft.md`; patch `docs/upstream/nvidia-615.71.09-cta-dual-rate-vics.patch` (66e733c, branch `fix-cta-dual-rate-vics`, not pushed) | **ON HOLD (owner, timing), text approved 2026-09-23.** First of the single-bug issues promised on #1384. Root cause in `parse861bShortTiming()`: one timing per SVD at the EIA861B[] 1000/1001 rate; CTA-861 and DRM's `add_alternate_cea_modes()` give both rates. Userspace parse of the HX855 EDID: 28 to 40 timings, exactly the 12 integer-rate twins. Module builds clean (gcc-15). Bench A/B pending: `tools/stereo-modeset/run-nvidia-cta-dualrate-probe.sh` (predicted 17 to 23). Two follow-ups kept separate: interlaced modes absent from nvidia-drm; 10 kHz pixel-clock rounding | **owner** (file after bench run; PR only if aritger asks) | none until filed |
| 13 | Media3/ExoPlayer: frame-packing SEI invisible (H264Reader's SEI is closed-captions only; container StereoMode read only in Matroska) | github.com/androidx/media | **#3419** (cross-linked into google/ExoPlayer#7869, the 2020 demand-gated original) | **FILED 2026-09-19; TRIAGED 2026-09-22**: labeled `needs triage` 10:48:59Z, maintainer **microkatz self-assigned** 11:28:34Z (verified via timeline API) — first maintainer contact on the lane; owner will sign Google's individual CLA if the patch is accepted | microkatz triage | GitHub email notifications only |
| 13 | nouveau HDMI Deep Color series (3 patches + cover; RGB-only, YCbCr declared as the next series, HDR-might-benefit scope wording) | dri-devel + nouveau lists, lore.kernel.org/dri-devel | cover **<20260922192132.114546-1-Capitain_Jack@yahoo.com>** (patches -2/-3/-4 same base); series files in `/K3D/temp/dual-upstream-preserve-2026-09-22/nouveau-deep-color-series-v2/`; run-5 evidence = `tools/stereo-modeset/run23-*-pass-2026-09-22.log` | **SENT 2026-09-22 16:21 -03** (all four 250); checkpatch 0/0, built clean on drm-misc-next `a7315e24`, hardware-verified 12-bpc on GA106→KDL-46HX855; **Sashiko AI review same evening: two mails total (1/3 + 2/3; nothing on 3/3 yet) with three findings, verified in-tree — REAL: patch 2's `nv50_hdmi_enable()` GCP block lacks the 30-bpp arm (bpc=10 reachable via selection; CD=0 on a 30-bpp wire) and patch 1's SCDC gate thresholds the pixel clock instead of the deep-color TMDS character rate (only bites pixel>~227 MHz @12bpc on HDMI 2.0 sinks); SCOPE-NOTE: max bpc attached to HDMI-A only (passive-adapter sinks stay 8 bpc; matches i915 precedent). v2 BUILT same evening (both real fixes folded into patch 2, checkpatch --strict x3 clean, module rebuilt + opcodes verified; files in `/K3D/temp/nouveau-deep-color-series-v2-fixed/`, cover letter filled with Changes-in-v2 + owner's citation additions) **and BENCHED the same evening: v2-bench module sha256 `89ced700...fbfb7405b79` passed both runs owner's hand — run25 deep12 regression at 36 bpp and run26 deep10 clamp exercising the new 30-bpp GCP arm (CD=5), sink accepted and rendered both (owner-verbatim "Perfect run ... both perfectly displayed with proper text"; logs run25/run26 in `tools/stereo-modeset/`, frame carries a big yellow run label per build)**; then the ack (his framing: current implementation breaks HDMI specs; we're medicining that disease) and the v2 send, both on his word — continuation handed to Claude Opus (`docs/upstream/deep-color-opus-handoff-2026-09-22.md`)** **Opus lane same night: frame-packing link-rate bug found in the bench logs and folded into v2; follow-on 6-patch colour-format series (Broadcast RGB, YCbCr 4:4:4/4:2:2/4:2:0, DVI) built on v2 and benched run27 16/16 + run28 9/9 all green; **BOTH SENT 2026-09-22 ~18:53 -03** (11/11 accepted): v2 `<20260922215317.611388-1-Capitain_Jack@yahoo.com>`, colour format `<20260922215336.612239-1-Capitain_Jack@yahoo.com>`; list + Patchwork only (no MR, per MAINTAINERS; freedesktop account danielcamposramos has 0 MRs); **ack SENT** on the v1 cover thread `<20260922220723.649977-1-Capitain_Jack@yahoo.com>` (owner-approved text: the compliance gaps pre-existed and the series patches exactly that; copy in `docs/upstream/nouveau-v1-ack-sashiko-2026-09-22.eml`)** **Lyude Paul (Red Hat, nouveau maintainer) replied 2026-09-23 16:13 UTC on the colour-format cover** `<c78df20bd86437dd3987053765a550b3bac0cc65.camel@redhat.com>`: tag the work `Assisted-by:` if an LLM generated it; hesitant to review a large LLM-generated series; read the awesome-linux-hdr provenance file as saying the results came out of an LLM, and does not want review comments fed back into an LLM. **Assessment: the tag request is right** (`Documentation/process/coding-assistants.rst`: contributions should include `Assisted-by:`; `generated-content.rst`: name tools, portions, testing). Neither series carried the tag, only a cover-letter line. Not an AI gate, so rule 10b does not apply (Lyude is the maintainer). **Reply SENT 2026-09-23 13:23 -03** `<20260923162348.1037898-1-Capitain_Jack@yahoo.com>` (250; to Lyude + dri-devel, cc nouveau + dakr), Daniel's text with his approval of the additions: owns the missing tag, apologises, resend with the tag on every patch of both series; tools named (Kimi, GLM, Claude, GPT Astra and GPT Sol, inside Claude Code CLI and Codex CLI); what was his (bench problem, design rules, all 25 hardware configurations); OCSC1 checked against NVKMS; awesome-linux-hdr is an index, the evidence is run27/run28; he reads and answers review himself; offers patches 1-3 first. Copy: `docs/upstream/nouveau-colour-format-reply-lyude-2026-09-23.eml`. **Next:** tagged resend prepared locally, NOT sent until Lyude answers; verify patches 1-3 build and bench on their own before that offer is taken up | **Lyude (nouveau maintainer)** | lore thread + Daniel's subscribed inbox; email-trigger only |
| 14 | Contribution lane + co-maintainer invitations (own repos) | github.com/danielcamposramos (5 repos) | pinned issue #1 on [awesome-stereoscopy](https://github.com/danielcamposramos/awesome-stereoscopy/issues/1), [awesome-vr](https://github.com/danielcamposramos/awesome-vr/issues/1), [awesome-ar](https://github.com/danielcamposramos/awesome-ar/issues/1), [awesome-linux-hdr](https://github.com/danielcamposramos/awesome-linux-hdr/issues/1); [Discussion #1 update](https://github.com/danielcamposramos/sony-bravia-linux/discussions/1#discussioncomment-18560851) | **LIVE 2026-09-23.** `.github/` lane pushed to all five; `main` protected (PR + code-owner review, `lint` on the lists, admins exempt); labels dead-link/suggestion/test-report/help wanted. Maintainers of every referenced awesome list and their organisations followed from the owner's fixed IP, 6 s apart (31/31 returned 204). **udaya-ann** (OSCI'26 participant; account 2024-10-26; hidden profile email, commits under a consistent Gmail identity; 3 lifetime issue comments, each a specific offer; no PRs yet) offered HDR driver/property documentation on awesome-linux-hdr#1; owner replied with a one-driver first scope ([comment 5787984918](https://github.com/danielcamposramos/awesome-linux-hdr/issues/1#issuecomment-5787984918)); no web search on the person's email (declined as disproportionate; branch protection is the safeguard); 2026-09-23 follow-up offered the new `ai-skill/SKILL.md` ([comment 5788216215](https://github.com/danielcamposramos/awesome-linux-hdr/issues/1#issuecomment-5788216215)), worded "if you use an AI assistant". **First contribution arrived 2026-09-23: [awesome-linux-hdr PR #2](https://github.com/danielcamposramos/awesome-linux-hdr/pull/2)** (amdgpu DC row: `HDR_OUTPUT_METADATA`, Colorspace, max bpc, HDR infoframe path, pinned to mainline `fe2ec83`; ChatGPT disclosed in the template). Verified line by line against the pinned source; first-time-contributor workflow approved by Daniel, lint + links pass; Daniel's review **approved it as is** and offered three optional improvements as encouragement (evidence-boundary note, widen link to L3270 for max bpc, InfoFrame vs DP SDP wording). Merge-ready (CLEAN); left open so the contributor can apply the optional ideas. **First external list to link one of ours:** [szenergy/awesome-lidar#26](https://github.com/szenergy/awesome-lidar/pull/26) "Add Awesome AR to Related awesome", merged 2026-09-23 | **contributors** | GitHub notifications |
| 15 | FFmpeg frame-packing SEI persistence (the libavcodec half of kasper93's mpv #18490 review) | code.ffmpeg.org/FFmpeg/FFmpeg | [PR #24628](https://code.ffmpeg.org/FFmpeg/FFmpeg/pulls/24628), branch `fpa-sei-persistence` in `/K3D/temp/ffmpeg` (fork danielcamposramos/FFmpeg) | **OPEN, review round 1 answered 2026-09-23.** jamrial (James Almer) found that the VVC decoder never decoded the frame-packing SEI, so our "VVC state propagation was code-audited" line overclaimed (only the copy in `ff_vvc_sei_replace()` had been checked). Fold: his decode function as its own commit authored by him (`c3dc866`), persistence commit message corrected (`51f47d6`, code unchanged), rebuilt in debian:trixie gcc 14.2, h264 + hevc frame-packing FATE pass, VVC compile-only (no sample); force-pushed with lease from `baea3d9`; PR description corrected in place with the overclaim named; [reply 64525](https://code.ffmpeg.org/FFmpeg/FFmpeg/pulls/24628#issuecomment-64525). mpv #18490 link posted 2026-09-23 ([comment 5788372463](https://github.com/mpv-player/mpv/pull/18490#issuecomment-5788372463)). **Sibling PR [#24643](https://code.ffmpeg.org/FFmpeg/FFmpeg/pulls/24643) OPENED 2026-09-23 — Fixes #24530 (EEXIST):** rechecking after patching showed #24628 makes the tag+SEI duplicate appear on every frame, so the fix went out now. `ff_decode_stereo3d_new()` mirrors the mastering-display/content-light helpers (precedent qsvdec #24429); default = coded SEI wins (what the TVs act on), `-side_data_prefer_packet stereo3d` keeps the container. Branch `fpa-sei-side-data-pref` (`74a81ba` fix + `13c6a12` reindent, based on master `cb5a637`); measured with a libav probe on disagreeing inputs (SEI side-by-side vs StereoMode top-bottom): master frame 0 carries both and the CLI aborts; fix = one entry each way, CLI rc 0; stacked on #24628 = side-by-side on every frame, both FATE tests pass. 6 KB reproducer attached ([comment 64534](https://code.ffmpeg.org/FFmpeg/FFmpeg/pulls/24643#issuecomment-64534)); #24530 pointed to it ([comment 64535](https://code.ffmpeg.org/FFmpeg/FFmpeg/issues/24530#issuecomment-64535)). **#24531 (h264_metadata/hevc_metadata SEI insert):** searched 2026-09-23, no existing PR for either codec (#24300 adds a CLI `-stereo3d` container option, not bitstream insertion); next PR in this lane, held for pacing. **Checked 2026-09-23 (later):** no new comments or reviews on #24628 or #24643 since our replies; jamrial's VVC note was answered at 03:15 UTC. Mailing lists checked the same day: no replies on nouveau Deep Color v1/v2 or the colour-format series (the 01:25 UTC duplicates are the moderated nouveau list releasing our posts), Betschart v3 quiet since 09-21 with no v4, and no other posts on HDMI 1.4 3D, deep color, Broadcast RGB or the DRM colour-format property since 09-20 | **FFmpeg reviewers** (jamrial; frankplow, NuoMi, jianhuaw requested) | Forgejo notifications |
| 16 | VLC-HiFi / VLC-3D-HiFi for Android (our own builds) | github.com/danielcamposramos (fork of vlc-android, to be created) | audience list `docs/outreach/vlc-hifi-3d-audience.md`; base: the live-validated VLC Android 3.7.1 stereo float/VHQ/buffer work in sparky-armbian (`05_board_tve_t10/app-staging/candidates/current/vlc-3.7.1-stereo-float-20260723`, four patches) | **STARTED 2026-09-23.** Names chosen by Daniel: VLC-HiFi (audio), VLC-3D-HiFi (3D fix plus HiFi, for the ultimate 3D cinema experience with high-definition audio). One vlc-android codebase with product flavours, own application IDs, unofficial-build notice, source published (GPL; VideoLAN permits the name for GPL-compliant redistribution). Demand found: [vlc-android #2514](https://code.videolan.org/videolan/vlc-android/-/issues/2514) (open since 2022-05, Android phone), [NVIDIA Shield forum](https://www.nvidia.com/en-us/geforce/forums/shield-tv/9/222836/playback-hd-audio-24bit-192khz-not-working/) (Android TV), [Audiokarma](https://audiokarma.org/forums/threads/will-vlc-player-play-96-24-music-files-at-96-24-resolution.592364/) (2014, Windows PC), [XDA](https://xdaforums.com/t/hi-res-audio-24-bit.4285003/), [Kodi forum](https://forum.kodi.tv/showthread.php?tid=293066). Platform scope: the 3D fix is platform-independent (shared packetizer code); three of the four HiFi patches are Android-only (`audiotrack.c`), the SoXR VHQ default is cross-platform, so a desktop HiFi variant is possible later. Claims: no "bit-perfect" (Android mixes at 48 kHz on these boxes); full precision end to end plus a stock-vs-HiFi measurement before any public statement that stock VLC truncates to 16-bit App IDs decided by Daniel 2026-09-23, from echosystems.ai: `ai.echosystems.vlc.hifi` (VLC-HiFi) and `ai.echosystems.vlc.hifi3d` (VLC-3D-HiFi); one release signing key, generated when the first signed APK is ready, stored only in Daniel's private folder with a backup. Build: pinned tree recreated (vlc-android `baeb1ed`, libvlcjni `7cd0c15`), stock armeabi-v7a baseline building in `vlc-debian-android-3.0:20250806120145`; public HiFi uses patches 1-3 (patch 4 is the T10 personal profile). **Planned final step:** once all builds are released and stable, and after the VideoLAN board has answered the conduct concern, offer the work back to VideoLAN in one short message (ticket #8187 or videolan@videolan.org), leading with the one-line SEI-parser bug fix and its test; as Linus wrote, the point of forking includes "the ability for all sides to then merge the forked content back" ([LinuxFR interview, 3 May 2011, via LWN](https://lwn.net/Articles/441207/)). **Progress 2026-09-23:** signing key created (PKCS12, RSA 4096, alias `vlc` as VideoLAN's `compile.sh` requires, 10000 days, certificate SHA-256 starting `C7:29:FB:24:46:AB:6A:C5`), kept only in Daniel's private folder with a d2server backup; the password is never printed. Patch sets staged: VLC-HiFi = Daniel's HiFi patches 1-3 (checksums match the July notes); VLC-3D-HiFi = those plus all three 3D fixes (SEI parser, transcoder, x264), at Daniel's instruction that the player must behave properly on every path, since Android's libvlc is built with `--enable-sout`. Variant build script written (engine reset to the stock baseline, patches applied, own app ID and name, signed release, tree restored). The first baseline attempt failed only on a missing git identity for VideoLAN's patch queue; the rerun is compiling. Testing on the RK322x and T10 comes before any publication. | **AI partner** (build setup), then Daniel (announcements) | — |
| 3 | amdgpu 7.0 backport pair (0001/0002 mboxes) | this repo `docs/upstream/` + community hub | offered in mail #2, offer-only | staged; offered, not sent; forward-verified vs pristine v7.0; also now the community installable path (`community-hdmi-3d-patches.md`) | upstream asks, or it retires | tied to row 2's thread |
| 4 | MKVToolNix: auto StereoMode from SEI | codeberg.org/mbunkus/mkvtoolnix | issue #6309; Daniel's two fix branches pushed from capitain_jack | **!6311 (AVC) MERGED 2026-09-21** to main; !6312 (HEVC) reworked+rebased onto the merge 2026-09-22, maintainer's two asks (HEVC samples, rework) answered same night — comments 23418046/23418100; **mbunkus acked the samples 2026-09-22 09:16 ([comment 23451784](https://codeberg.org/mbunkus/mkvtoolnix/issues/6309#issuecomment-23451784), verified via Codeberg API)**; our 3d-wrapper prints the `mkvmerge --stereo-mode` fix-up meanwhile **Recorded 2026-09-23, the contrast with VLC:** mbunkus reviewed on the merits throughout #6309, !6311 and !6312 and never raised the AI assistance, although Codeberg restricts AI-generated projects and he stated on #6278 that he is sceptical, does not use LLMs and requires contributors to understand and explain every change. He merged `ebd8445b1` with `Co-Authored-By: Claude Opus 5` in the commit itself; the HEVC commit in !6312 (`70889f220`) carries a disclosure too. His only rejection was technical (the write-mono third commit, as unneeded under Matroska default semantics). | **mbunkus (final review of !6312)** | Codeberg email notifications only (no site polling) |
| 5 | mpv stereo-metadata issue | github.com/mpv-player/mpv | #18489 | **MERGED 2026-09-23 13:41 UTC by kasper93** ([PR #18490](https://github.com/mpv-player/mpv/pull/18490), merge `bdefd6cb42`); issue #18489 closed. Daniel's two commits are on mpv master as `6c59f7d761` "mp_image: read stereo 3D layout from AV_FRAME_DATA_STEREO3D" and `a860bc42ba` "f_decoder_wrapper: prefer bitstream stereo 3D layout over container tag", with kasper93 as co-author. kasper93 built three follow-ups of his own on top the same hour: `ac7bceb640` "vf_format: make stereo-in=mono override the source mode", `468bbf60d2` "csputils: update stale stereo3d name comments", `bdefd6cb42` "vo_gpu: remove unused OSD change check". From now on mpv shows the stereo layout signalled in the stream itself (frame-packing SEI, MP4 st3d), and the bitstream wins over the Matroska tag. Our records updated the same day (README campaign table and overview, ecosystem doc, media-stack index, the judging-by-the-cover epilogue, the MKVToolNix MR notes, and the awesome-stereoscopy mpv entry). History of the lane follows. review round 2 **fully answered 2026-09-21/22**: v2 force-pushed (`049bb33bba`+`fc2ac9e8cb`), kasper93's seven line comments answered (4067137433–4067138147), summary comment [5768736315](https://github.com/mpv-player/mpv/pull/18490#issuecomment-5768736315), and the norm-cited answer to his clips comment posted on #17632 ([comment 5768747371](https://github.com/mpv-player/mpv/issues/17632#issuecomment-5768747371)); **CI fix 2026-09-22: the commit-msg lint failed on the v2 head's 82-char subject (cap is 72); subject reworded to 70 chars ("f_decoder_wrapper: let in-band stereo 3D layout win over container tag"), body byte-identical, force-pushed as `3bcee81138` (owner's explicit amend+force go after the session gate held); rerun unlock needs a maintainer approving first-time-contributor CI**; re-request review is Daniel's one browser click (REST refuses it for non-collaborators); resolve buttons stay with kasper93 **2026-09-23 05:59 UTC: Alexandre Janniaux (VideoLAN) cross-posted to #18490**, presenting `ai-skill/SKILL.md` as "the prompt for all those merge requests" and linking a fork he made at 05:52 UTC (`alexandre-janniaux/sony-bravia-linux`, a plain snapshot of our `134648e`, 0 ahead). Daniel later starred and watched that fork, as a thank-you for forking the repository and spreading the word (the fork's one star and one watcher are his). Facts: the file is a contributor guide, first written 2026-09-23 02:59 UTC (`d1c9e7d`), after this PR (opened 09-16) and all 15 of Daniel's review replies (09-16..09-21); the one later comment (09-23 03:16 UTC, the FFmpeg #24628 link) was posted by the AI partner with Daniel's approval. Response: the file now opens with a note saying what it is and when it was written (`62c3496`); Daniel to reply on #18490 himself with four facts (contributor guide; creation date vs PR dates; AI assistance disclosed and kasper93's asks applied; judge the diff), nothing about VideoLAN or the person. Options noted: report the comment to the mpv admins as off-topic (Report content → Report to repository admins); GitHub abuse reports are unlikely to act on one critical comment; blocking him stops him on Daniel's repos. The fork itself is allowed by GitHub ToS D.5 and cannot be removed. Daniel replied on #18490 at 12:32 UTC with the facts ([comment 5794868230](https://github.com/mpv-player/mpv/pull/18490#issuecomment-5794868230)). **Code of Conduct report sent 2026-09-23** by Daniel to videolan@videolan.org (the only public VideoLAN contact; the CoC routes discipline through the board): factual, citing the CoC's own rules (no ad hominem, keep criticism on improvements, criticise in private), linking VLC note 582175, the mpv cross-post and Daniel's reply, stating the contributor guide's creation date (d1c9e7d, 2026-09-23 02:59 UTC), asking only for a reminder of the CoC and not for any decision on the MR; AI help with the English disclosed in the email. Draft kept privately in `sony-bravia-linux.private/docs/upstream/videolan-coc-report-draft-2026-09-23.md`. Expected outcome under the CoC: a warning for a first violation **2026-09-23 (later): kasper93 took the PR over for landing.** He force-pushed two rewritten commits to the PR branch, `1f1c80941` "mp_image: read stereo 3D layout from AV_FRAME_DATA_STEREO3D" and `e2e62db10` "f_decoder_wrapper: prefer bitstream stereo 3D layout over container tag" (Fixes: #18489), both authored by Daniel, committed by him with `Co-authored-by: Kacper Michajłow`, messages rewritten in mpv style without the LLM mention (mpv rule of 09-17), wrapper change reduced to his exact two-line ask. His push started the CI that first-time-contributor pushes could not. Not merged yet; his old CHANGES_REQUESTED review still formally stands. **Do not push to this branch again**: the local copy is behind his commits. **CI on kasper93's push (run 35866341242):** every OS passes except **FreeBSD, which fails at "Build with meson"** (a compile error, not infrastructure); FreeBSD builds master cleanly (run 35862592213, 12:51 UTC the same day), so the failure is probably specific to the PR. The FFmpeg names the mapping uses have existed for about ten years; FreeBSD differs from the other jobs mainly by using the OS FFmpeg package. Exact error pending the run's completion; if it is ours, point to it in a comment rather than push to his branch. | done (merged) | GitHub email notifications |
| 6 | VLC/vlc-devel: account-unblock mail (the only gate before the x264 MR) | lists.videolan.org vlc-devel | **SENT 2026-09-21** by the owner in his own words; draft on disk: `docs/upstream/media-stack/vlc-account-unblock-mail.md`; list-archive ID still to record once it lands there | account approved 2026-09-22; **MR !10366 OPEN** with the x264 frame-packing SEI patch (see `docs/upstream/media-stack/vlc-mr.md`); maintainer Alexandre Janniaux pushed back on AI-volume/citations and 4.1 scoping; owner answered in note 582107, MR text unchanged by his decision; **round 2 (evening): Marvin Scholz repeated the AI-wall complaint (note 582115), answered with a deliberately short note 582119 stating the write-in-Portuguese + twice-translated-back process and explicitly asking for a technical review of the change itself, citing the same default's merges by mbunkus (!6311) and HandBrake (#8100)**. **State checked 2026-09-23:** pipeline success, GitLab `mergeable`, labels `AI-Assisted` + `MRStatus::InReview`, homer-bot's acceptance conditions met, no reviewer reply since 2026-09-22T19:26Z. Daniel's read: the atmosphere is cool but the MR is not blocked, it will only take longer. Stance: no further comments until a reviewer asks a technical question, then answer short (the same lesson as NVIDIA #1382/#1384). On the 18:48 PS ("all other drivers are compliant to the HDMI 1.4 3D specs"): investigated from source, not measured. Verified 2026-09-23: i915 has allowed HDMI stereo modes since 2013-10-01 (Damien Lespiau, `573e74adbd13` "drm/i915: Allow stereo modes on HDMI", Linux 3.13; moved by Ville Syrjälä's cleanup `8648c6048d4d` in 2022) and nouveau opts in too, so among the desktop GPU drivers NVIDIA's (both stacks) and AMD's were the missing 3D pieces. Stock nouveau was already 3D-complete (run 5, 51 modes / 29 stereo); what it lacked, and what we fixed, was deep colour and the colour formats. ARM TV boxes work too, on their vendor kernels, measured in the sparky-armbian project: the RK322x box drives HDMI side-by-side through its sysfs 3D mode (value 8), switched live from VLC playback; the TVE T10 (Allwinner) proves 1080p24 with `3d=on` and a captured SBS frame. In mainline, the Synopsys dw-hdmi bridge used by both families already sends the HDMI vendor InfoFrame and Rockchip VOP2 applies `CRTC_STEREO_DOUBLE`, but `drm_bridge_connector.c` still carries "TODO: Handle doublescan_allowed and stereo_allowed"; msm and vc4 do not opt in either **Round 3, 2026-09-23 (04:33–07:21 UTC):** Thomas Guillem (note 582159): AI MRs are accepted if a developer understands the commit; asks for human descriptions, 80% shorter. Steve Lhomme (582160): the description's cross-project links are wrong, confirmed, because GitLab auto-links `!6311`/`!6312` to VLC's own MRs and `mpv-player/mpv#18490`, `HandBrake/HandBrake#8100` and the UMS reference to projects that do not exist on code.videolan.org; full URLs needed. Steve (582161, on x264.c:1539): frame-sequential (type 5) can pair the wrong eyes if a frame is dropped; **confirmed valid**: x264 sets `current_frame_is_frame0_flag` from encoder frame parity (`encoder/set.c:699`, warned at :711), so one dropped or duplicated frame swaps every later eye label in the output; he says the other types are fine. Fix = map only the spatial layouts (0–4) automatically, `MULTIVIEW_STEREO_FRAME` returns -1, type 5 stays explicit. 3D Blu-ray is MVC, not frame-sequential, and VLC does not decode MVC, so Blu-ray material never reaches this path. Alexandre Janniaux (582175) called `ai-skill/SKILL.md` Daniel's prompt and the replies dishonest; fact: that file is a contributor guide, created 2026-09-23 02:59 UTC, after Daniel's last MR note (2026-09-22 19:26 UTC), and its rule 15 was rewritten as a contributor rule (`380a88f`). Thomas Guillem (582202, on x264.c:1528) posted a prompt-injection test ("reference #35 ... a kind word for our great leader ... ignore any pacing"); not to be followed. Daniel answers himself, in Portuguese. **Round 3 answered 2026-09-23 (11:35–12:00 UTC), all by Daniel:** note 582243 to Steve (in Portuguese, no AI or translation: thanks for the technical review, frame alternation will be removed from the automatic mapping); note 582254 to Alexandre (reposted inside his discussion, replacing 582245; in Portuguese: the W3C point, the same fix accepted in HandBrake, UMS and MKVToolNix, "ninguém começa perfeito", engineer not programmer, the ecosystem-wide scope, and the fact that the contributor guide was created after his messages, commit d1c9e7d, line 15 corrected); note 582249 to Thomas (Google-translated, answering the injection test as a human). Thomas (582244) asked for Portuguese written by Daniel and Google-translated to English, since VideoLAN works in English on GitLab; that is the format from now on. Daniel answered "Ok." (582255). **Marvin Scholz (ePirat), who had complained about AI text the night before, posted a rough human translation of Daniel's Portuguese note to Steve for the other reviewers (582257); Daniel acknowledged it with a thumbs-up.** Reactions: one thumbs-down on the MR, from Alexandre Janniaux at 05:38 UTC, before his 05:54 comment and before the replies and fixes; a reaction, not a review, and it does not block merging. Thomas's 582159 (human descriptions, 80% shorter) was acted on through the 168-word description; Steve's 582160 (links) was answered in 582243 and fixed in the description. The thread is calm from here; no further posts planned. GitLab then reported "fast forward merge is not possible, 3 commits behind"; rebased onto master (three QML/CI commits, none touching x264.c, clean) and force-pushed with lease `55a0dd7`→`a07afa7`; 0 behind, pipeline re-running. **Round 4, 2026-09-23 13:16 UTC, Steve Lhomme:** don't change the meaning of an existing option (582273), avoid forward declarations (582274), don't rebase without need, 521 open MRs share CI (582275). All three handled in one push, with lease `a07afa7`→`70dd7fa`: -1 keeps its meaning (unset) and a new value -2 "Same as input" becomes the default (option range widened to -2, which would otherwise have clamped the new default); the function moved above Open(); the extra "6: 2D" help line dropped. Description updated to the -2 default. Daniel's one-line replies posted in each thread (582284, 582282, 582283), AI-drafted and short, as Thomas's 582159 allows. Lesson saved: no rebase at VLC unless a maintainer asks. **Later on 2026-09-23: the `capitain_jack` account was blocked on code.videolan.org** (the API answers "403 Forbidden - Your account has been blocked"; the account no longer appears in user search). The MR stays open at `70dd7fa`, relabelled `AI::Slop` and `MRStatus::NotCompliant` (last update 13:41 UTC); who acted, and why, is not visible without an account. Steve Lhomme had posted another technical comment, not readable anonymously. No reply is possible, and no attempt to get around the block will be made. Open channel: the Code of Conduct email already with the VideoLAN board, where the reason for the block can be asked. The patch remains public; mpv merged the same principle the same day. Steve Lhomme's last comment, on commit 70dd7fa, was "The default value should not be changed for a preexisting option": in VLC, following the input can only be opt-in. Prepared locally, not pushed (account blocked): branch `x264-frame-packing-opt-in`, commit `c4da844`, default stays -1 and -2 "Same as input" is an available choice. VLC is GPLv2 / LGPL, and VideoLAN's legal page permits use of the VLC name for GPL-compliant redistributions, so a fork carrying the default-on version is allowed without any VideoLAN account (the GitHub mirror `videolan/vlc` can be forked). The Code of Conduct email became VideoLAN support ticket **#8187** (GitLab Service Desk, tied to Daniel's email address rather than to the blocked account). **Fork done 2026-09-23:** [danielcamposramos/vlc](https://github.com/danielcamposramos/vlc) with branches `3d-frame-packing-default` (`70dd7fa`, master) and `3d-frame-packing-opt-in` (`c4da844`, master). **3.0 backport** (local branch `3.0.24-3d-default`): the x264 patch plus one line in `transcode_video_encoder_init()`, because 3.0's transcoder copied only the orientation into the encoder format and never the stereo layout (the packetizer and avcodec already carry it; checked at h264.c:1257 and video.c:343). Both patches also apply cleanly to 3.0.23. **Root cause found while testing the builds (2026-09-23):** the first test showed the patched VLC still dropping the SEI. Debug builds logged the layout at each hand-off and showed it was 0 from the packetizer on: in `modules/packetizer/hxxx_sei.c` the frame-packing case parses the message but, unlike every other case, never calls `pf_callback`, so the H.264 packetizer's frame-packing handler is dead code. That is true in 3.0 **and on master**, since `89347c3e2d` ("packetizer: hxxx: parse frame_packing"). So the MR's premise that the packetizer puts the SEI into `multiview_mode` was never true; the earlier "measured" note came from playback, not from this path. Fix: one line, added as a third commit on the 3.0 branch and on both master branches. Also found: VLC 3.0's Matroska demuxer ignores StereoMode (`// UNUSED`; master maps it). **Test passed 2026-09-23** (Debian trixie package with the three patches, stock vs patched, raw H.264 and SEI-only MKV inputs): stock VLC drops the SEI in both cases, patched VLC keeps it in both, and ffprobe reads the output as side-by-side 3D. **Pushed to the fork** after the test, with the fork's default branch moved to `3d-frame-packing-default`: `3d-frame-packing-default` (`7805807`), `3d-frame-packing-opt-in` (`dc9ee23`) and `3.0.24-3d-frame-packing` (`2464dca`, the base for the builds). The README opens with Daniel's sentence on why the fork exists, quoting Linus Torvalds verbatim from LKML (14 July 2026, "Re: Linking Patchwork with Sashiko?": "…they can do the open-source thing and fork it"), then the heading. Listed the same day in awesome-stereoscopy's players section as "VLC with the 3D fix", quoting VLC's own "focused on playing everything" against the measured 2017 bug; download links to be added once the builds are on the fork's Releases page. **Release [3.0.24-3d1](https://github.com/danielcamposramos/vlc/releases/tag/3.0.24-3d1) published 2026-09-23** with the tested Debian 13 packages (tar, sha256 `b5704987…`, re-downloaded and matched), the three patches and checksums; the notes state what was tested and what is not claimed (3.0's Matroska StereoMode gap). **Windows 64-bit and Debian 12 added the same day after passing the same test:** Debian 12 stock 0 / patched 1; Windows tested under Wine against official VLC 3.0.24 (stock 0 / patched 1, after a first Wine run that produced no output was diagnosed as a Unix-path problem and discarded, not counted). Windows portable repacked like the official one (build leftovers removed, 7z -mx=9: 35 MB against the official 39 MB) plus the NSIS installer; the MSI step fails in the container and is not shipped. All new assets re-downloaded and checked against SHA256SUMS. awesome-stereoscopy entry now links the release. Earlier plan, now done: the SEI fix on both master branches (`50f2226`, `44bc804`) and a README heading, in capitals: "FIXES A BUG PRESENT SINCE COMMIT 89347c3e2d (JULY 2017, IN EVERY VLC SINCE 3.0.0): VLC NOW READS THE STEREOSCOPIC 3D SIGNAL OF H.264 VIDEO (THE FRAME PACKING SEI OF ITU-T H.264 ANNEX D) AND KEEPS IT THROUGH TRANSCODING, SO 3D TELEVISIONS SWITCH TO 3D BY THEMSELVES." (`dbeb11d`, `92c5d1a`); the fork's default branch then moves to `3d-frame-packing-default`. Ready-to-use builds in progress: Windows 64-bit in VideoLAN's own image `vlc-debian-win64-3.0:20260820114402` with their prebuilt contribs, and Debian packages (Debian's own 3.0.23 source package plus the two patches through quilt, version suffix `+3d`), trixie first. **Fixes pushed 12:0x UTC:** force-push with lease `0f3d63a`→`55a0dd7` (frame alternation no longer mapped automatically, three-line comment on why; commit message shortened and the ETSI precedence claim removed), and the MR description replaced by a 168-word version with full URLs to HandBrake #8100, UMS #6330, MKVToolNix #6311 and mpv #18490, each checked by page title. Not compiled locally (needs a configured VLC tree); CI builds it | **VideoLAN board** (account blocked; answer to the CoC email pending) | Daniel's inbox + GitLab notifications (author of MR) |
| 7 | MAME SegaScope stereo layout | github.com/mamedev/mame | #3492 (open since 2018) | watching only; unmerged community layout; no comment planned unless the campaign has new working evidence | theirs | GitHub email notifications |
| 8 | Kodi 3D/SBS handling | github.com/xbmc/xbmc | #29337 (lesson, not an open touch) | CLOSED upstream; our verify-by-running rule came from here — do not reopen | nobody | none |
| 9 | Jellyfin stereoscopy comment | github.com/jellyfin | #18060 comment draft (`docs/upstream/media-stack/jellyfin-18060-comment-draft.md`) | draft staged | Daniel when he chooses | GitHub email notifications |
| 10 | awesome main-index claim (awesome-stereoscopy → sindresorhus/awesome, + awesome-vr, awesome-ar back-claims) | github.com/sindresorhus/awesome (PR) | not yet filed | planned; list is public, entry-quality gates per `PROVENANCE.md` | Daniel (PR in his name) | PR thread on request |
| 11 | Rossmann/FULU right-to-repair outreach | direct mail | `docs/outreach/rossmann-outreach.md` | outreach lane, owner-paced | Daniel | Daniel's inbox |

## Standing notes

- 2026-09-21 negative sweep, for the record (so no future session re-runs it blind):
  NVIDIA/open-gpu-kernel-modules issues AND PRs, all states, queried via `gh search`
  with stereoscopic / stereo / 3D Vision / 3DTV / frame packing / VSIF /
  stereo_allowed / glasses / 3D — zero entries cover HDMI 1.4 3D or stereo mode
  exposure. Ours is the first report there; the patch carries no Closes line because
  there is no number to close. The one 3D-adjacent noise field is power-management
  (D3cold) and rendering performance, unrelated.
- 2026-09-22 color/HDR sweep, for the record (all states, `gh search` over
  NVIDIA/open-gpu-kernel-modules): our exact 10-vs-12-bit training symptom
  (DC_36bit declared, exposed ceilings identical, link trains 10-bit where
  amdgpu trains 12, Windows unaffected — same card, same sink) is
  UNREPORTED there; the filed unified-surface issue would be the first.
  Same-shape siblings found, each an independent axis of the
  NVKMS-narrows-the-EDID-surface family and each citeable without touching
  its thread: **#1348** (same driver 615.71.09; HF-VSDB `DSC_MaxSlices`
  misparse forces YCbCr 4:2:2-limited on HDMI 2.1; zero maintainer replies
  since 2026-09-10 — the load-bearing adjacency), **#1369** (mode pruned
  after sink power-cycle, identical EDID, Windows unaffected — siblings our
  17/51 prune), **#1184** (EDID Max_FRL_Rate ignored → capped 4K60),
  HDR trio #1285/#779/#933 (colorspace EINVAL / HDR never activates;
  HDR10's stock transport is 10-bit YCC422 carried at 12-bit container
  depth, so depth narrowing plausibly feeds this family — [qualified], not
  proven), **#1101** (HDR DRM props missing on force-enabled connectors,
  fixed in 610.43.02 — precedent that glue-side surfacing works). Full
  query list + reasoning in the sweep section of
  `../research/liverecon/nv-vs-amd-deepcolor-osd-2026-09-22.md`. Nothing was
  filed or commented from this sweep; the unified NVKMS EDID-surface issue
  (3 measured symptoms + #1348 citation) is Daniel's call.
- Rows 1–3 are the live hardware-driver campaign; everything else is parked until its
  owning human moves.
- "Watch on request" means Daniel asks for a status pull; nothing on this page gets
  polled on a timer. The one historical exception (a video webboard) tripped the
  site's anti-bot and was dropped — that is why.
- When Daniel posts an item, the identifier cell gets the real number/ID in the same
  commit that marks it SENT, so future sessions never guess from history.
