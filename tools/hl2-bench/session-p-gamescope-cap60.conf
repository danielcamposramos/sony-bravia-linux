# Swim investigation, 2026-09-25: p15 (game free-running inside gamescope)
# cured the swim, but the module's pointer confinement (SDL_SetWindowMouseRect
# to the 640x480 UI sheet) pinned the view and the menu cursor to the
# sheet's bottom-right corner inside gamescope. Same run with the confinement
# off (module build 18d39bed turns it off by itself inside gamescope).
# p17 caps the game at 60 fps in DXVK instead of vsync, to see if the cure
# survives at 60.
# Run with: PLAY_PRESENT_INTERVAL=0 SVRTV_EXTRA="SVRTV_ANGLELOG=1" hl2-suite.sh session-p-gamescope-noconfine.conf
# p17 adds: DXVK_EXTRA="d3d9.maxFrameRate = 60"
# gpu: nvidia
p17-vk-sbs-gamescope-cap60      220  vulkan  sbs-gamescope   0  d1_town_01  play  HDMI-A-1
