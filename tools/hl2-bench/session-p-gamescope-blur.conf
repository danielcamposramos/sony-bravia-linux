# 2026-09-25: motion blur back on, in the cured setup (SBS through the 3D TV
# gamescope, output IMMEDIATE, game free-running, fence off, sprite fix,
# anisotropic 16x). Source's blur keeps its previous view in statics shared
# by both eyes (viewpostprocess.cpp), so the eyes may blur differently; this
# checks whether that is visible now that pacing is clean. The blur value is
# set on the launch line and printed in the console log.
# Run with: GAMESCOPE_BIN=$HOME/.local/bin/gamescope-3dtv PLAY_PRESENT_INTERVAL=0 SVRTV_EXTRA="SVRTV_ANGLELOG=1" HL2BENCH_CMDS="sv_cheats 1;viewmodel_fov 90;mat_forceaniso;mat_motion_blur_enabled" HL2BENCH_ARGS="+mat_forceaniso 16 +mat_motion_blur_enabled 1" hl2-suite.sh session-p-gamescope-blur.conf
# gpu: nvidia
p20-vk-sbs-gamescope-blur   220  vulkan  sbs-gamescope   0  d1_town_01  play  HDMI-A-1
