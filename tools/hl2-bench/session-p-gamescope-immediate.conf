# 2026-09-25: p16a (free-running in gamescope, fence off, sprite fix,
# anisotropic 16x) with the 3D TV build of gamescope: its nested output
# presents with IMMEDIATE instead of the hard-coded FIFO
# (/K3D/temp/gamescope-3dtv, nested-present-mode.patch). Daniel felt the
# remaining small drops as mouse lag in p16.
# (p16b asked for MAILBOX; NVIDIA does not offer it for this window, so it
# fell back to FIFO.)
# Run with: GAMESCOPE_NESTED_PRESENT_MODE=immediate GAMESCOPE_BIN=$HOME/.local/bin/gamescope-3dtv PLAY_PRESENT_INTERVAL=0 SVRTV_EXTRA="SVRTV_ANGLELOG=1" HL2BENCH_CMDS="sv_cheats 1;viewmodel_fov 90;mat_forceaniso" HL2BENCH_ARGS="+mat_forceaniso 16" hl2-suite.sh session-p-gamescope-immediate.conf
# gpu: nvidia
p16c-vk-sbs-gamescope-immediate  220  vulkan  sbs-gamescope   0  d1_town_01  play  HDMI-A-1
