# Session C: the wiz3D reference. HL2's Windows build under Proton, plain
# (vr=off) and with wiz3D 0.3.1 (vr=wiz: dx9/x86, SideBySideOutput, installed
# for that step only by wiz3d-setup.sh and removed after it).
# Prerequisite (Daniel, in Steam): Half-Life 2 > Properties > Compatibility >
# Proton, so Steam installs the Windows build. This is C1, today's build,
# which wiz3D has not listed. If it does not render, C2 is the steam_legacy
# beta branch, where wiz3D lists HL2 as "Mostly Working" (shadow issues, use
# -game); the demo will not play there, so C2 is views only.
# gpu: nvidia
# display: HDMI-A-2
# columns: id  app  renderer  vr(off|sbs|tab|wiz)  runs  frame  kind(view|timedemo|frame|watch)
c01-win-2d-view   220  dx9  off  0  90         view
c02-win-wiz-view  220  dx9  wiz  0  90         view
c03-win-2d        220  dx9  off  2  0          timedemo
c04-win-wiz       220  dx9  wiz  2  0          timedemo
c05-win-2d-frame  220  dx9  off  0  3000       frame
c06-win-wiz-frame 220  dx9  wiz  0  3000       frame
